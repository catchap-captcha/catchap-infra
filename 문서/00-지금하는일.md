# 지금 하는 일 — 살아있는 작업 맥락

★**이 파일은 대화가 압축돼도 살아남는 기억이다.** 새 작업을 시작하기 전에 **먼저 읽는다.**
길어지면 **시키기 전에 스스로 갱신한다.**

★★**왜 이 파일이 생겼나 (0805)** — 사장님 지적: *"너 자꾸 벗어나네"*. 실제로 벗어났다.
`00-아키텍처-설계.md` 2-E 에서 **(가)** 로 정해 놓고, 압축 뒤에 그걸 잊고 **(나)** 를 하려 했다.
**요약본은 「무엇을 했나」는 옮기지만 「왜 그렇게 정했나」는 떨어뜨린다.**
근거가 빠지면 다음 턴의 내가 같은 문제를 다시, 그리고 **다르게** 푼다.

**최종 갱신** — 2026-08-12 밤

---

## ✅★★★0812 — 반납 대비 정리 · 배포 복구 · 미디어 격리

### 🚨가장 중요한 것 — 내가 배포를 통째로 깨뜨렸고 적대적 점검에서 찾았다

```
06:52~07:23  ★다섯 저장소의 배포가 전부 failure 였다
원인         태그 정리 단계를 갈아 끼우며 ★옛 단계 몸통 8줄을 안 지움 → 중복 키
왜 못 봤나    PyYAML safe_load 는 중복 키를 ★조용히 허용 (GitHub Actions 는 거부)
             → "YAML 파싱해서 확인했다"는 내 보고가 ★거짓이었다
어떻게 찾았나  실행 이름이 워크플로 이름이 아니라 ★파일 경로로 찍혀 있었다
고침         ★중복 키를 거부하는 검사기를 만들고 ★그게 깨진 파일을 잡는지 먼저 확인(대조군)
             PR 백엔드#47 · 행동AI#19 · 캡차#27 · 프론트#84 · STT#12
```

★**교훈 — 「긍정 결과도 측정기부터 의심한다」.** 검사기가 깨진 것을 못 잡으면 통과는 무의미하다.

### ✅한 일

```
자산 보전   캡차 자산 2.05GB + DB 8표 → GitHub Release assets-20260812
            옛 GPU 소스 26개 → catchap-records
            ★catchap_captcha 표구조 21개 → catchap-infra (라벨링 표 7개는 ★어디에도 없었다)
502 없애기  preStop sleep 5 를 5개 배포본에 → ★실제 롤아웃 중 2,727요청 실패 0
Valkey     설정·CA·비밀값까지 배포 완료 (앱 코드는 민서님)
자원 정리   서버 8대 · 보안그룹 1 · 퍼블릭IP 5 · 서브넷 2 · 감시항목 1 삭제
미디어 격리  ★catchap-media-prod-team1 신설 · 3,476개 15.68GB 이전 · 옛 사본 삭제
```

### ★확정된 제약 (다시 시도하지 말 것)

```
레지스트리 태그 삭제   ★API 불가 — 토큰이 pull/push 만 준다. ★콘솔로만
서브넷 삭제           ★API 불가 (rule:delete_subnet disallowed). ★콘솔로만
서비스 계정 역할       ★생성 후 변경 불가 → 재생성뿐 → ★kms11 명단에서 빠진다
Secrets 뷰어          ★시크릿 「값」을 못 읽는다 → 앱은 ★매니저 + KMS 사용자가 최소
버킷 역할 그룹 매핑    ★새 버킷에도 자동으로 붙는다 → 버킷만 나눠선 격리가 안 된다
                     ★매니저는 뷰어를 포함 → 뷰어까지 빼야 읽기도 막힌다
콘솔 조작             ★chrome-devtools 자동화 크롬만 됨 (세션이 ★탭 단위)
```

### ⬜남은 것

```
⬜ 최종본 문서 (심사·강사님 제출용)
⬜ 행동AI 학습 데이터 74,670행 — 백업에 없음. 성원님과 결정 (개인정보 논의 필요)
⬜ 라벨링 원본 jsonl 147MB — ★영구 손실 (서버를 지웠다). 결과물은 DB 에 있음
⬜ 인증서 2026-10-20 만료 — 수동 갱신
⬜ 미사용 부동 IP 3개 — ★전부 남의 팀 것으로 판정. 건드리지 않는다
```

---

## ✅★★0811 — 캡차 스키마 검사 + ★조용히 멈춰 있던 배포를 고침

```
✅ 캡차 스키마 배포전 검사   k8s/captcha/20-schema-check-job.yaml (PreSync 훅)
                            ★「적용」이 아니라 「검사」로 정함 — alembic 없음 ·
                            schema.sql 의 ALTER 6개가 재실행 불가 · DDL 권한은 일부러 뗀 것
                            변이 시험으로 무는 것 확인(없는 표·없는 칼럼 둘 다 잡음)
                            → 30-작업이력/90-0811-캡차-스키마-배포전-검사/

🚨★내가 낸 버그를 찾아 고침       빌드는 성공인데 ★매니페스트 태그가 안 바뀌고 있었음
                            ★민서님 병합본(#14)이 하루 가까이 미배포 상태였음
                            원인 = 옛 셸 9줄을 덜 지워 changed=false 가 덮어씀
                            (GitHub Actions 는 같은 출력 키를 ★뒤엣것이 이긴다)
                            고침 + set -u · 다른 저장소 4곳은 정상 확인
                            → 30-작업이력/91-0811-태그갱신-조용한실패/

✅ 전 과정 한 바퀴 검증       병합 af99c45 → 빌드 → 태그(두 파일 함께) → ArgoCD
                            → 검사 Job(새 태그로 통과) → 파드 af99c45 x2 · 재시작 0
                            민서님 번들 index-TRfoyViy.js 가 파드 안에 있는 것까지
                            (옛 커밋엔 404 라는 대조군과 함께)

✅ 문안 3건 ★전달 완료(0811 오후)  ①민서님 ②성원님 2차 ③팀 전체 공지
                            문안 = 91-팀공유/21-0811-보내실-문안-3건.md

✅ 옛 시크릿 버전 정리(0811 오후)  옛 버전 12개가 ★전부 읽히는 상태였음
                            → 11개 폐기 · 1개 정지(회전한 것) · 기본 버전은 안 건드림
                            ★캡차·백엔드·행동AI 재시작해 금고 읽기 정상 확인
                            → 95-0811-최종상태/02-Secrets-KMS/
✅ 옛 서버 4대+옛 GPU 정지     사장님이 0811 오후 직접 (Team1-VM-1~4 · Team1-GPU-01)

⬜ ★secrets_loader 에 429 재시도 없음  파드가 한꺼번에 뜨면 기동 실패 가능(0811 발견)
⬜ 최종상태 전수 캡처          12개 서비스 중 IAM·시크릿·VM 일부만 함
⬜ 기록 정리 → catchap-records
⬜ 태그 1427147 삭제(2.65GB)  새 STT 이미지가 하루 이틀 멀쩡한 뒤에
⬜ VM 스냅샷 0개               콘솔 작업
⬜ 옛 계정 catchap-captcha-object-storage 액세스 키 삭제 확인
```

---

## ✅★★★0809 21:47 — ★컷오버 완료. 남은 것은 「옛 서버 내리기」입니다

```
✅ DNS 전환        @ = A 210.109.55.233 · www/api/captcha = CNAME (LB 두 IP)
                   ⚠️루트만 CNAME 불가라 A 하나 — 이중화 없음(0804 수용한 절충)
                   ⚠️가비아 포워딩은 ★못 씀(HTTPS)·TTL 최소 ★600
✅ 검증            8개 경로 정상 · 루트→www https·경로보존 · 글쓰기 새 DB 확인
                   옛 백엔드 요청 2분 만에 ★0건 · ★무중단

✅ DB 3개 재이전    129표 중 ★126표 완전 일치 (나머지 3 = 일부러 제외한 모니터링)
✅ 모니터링 이력    ★새 쪽이 상위집합 — 덮지 않고 옛것 196행만 합침 · 멱등성 증명
✅ 미디어          3,473개 16,590,309,184바이트 ★완전 일치 (증분 0)
✅ 루트 평문강등    LB 리스너에 XFProto+XFPort 켜서 수정 (★하나만 켜면 더 나빠짐)
✅ www/captcha-api  옛 환경과 동등하게 인그레스 추가

🔶 옛 서버 내리기   ✅정지(0811) · ✅3306 차단(0812) · ⬜삭제(반납)만 남음
                   ✅①정지 →✅②차단(퍼블릭IP 5개 해제 + 규칙 3줄 삭제) →⬜③삭제
                   ⚠️옛 DB 비밀번호가 git 이력에 남아 있음
                   ⚠️옛 GPU 는 sw님 466건 때문에 따로 판단
⬜ SW님 안내        참여자 수집이 ★새 DB 로 옮겨졌음 — 보시는 곳을 바꿔야 함
⬜ 캡차·행동AI 금고  로더 이식 (92-킵-대기/12)
```

---

## ✅★★★0808 — 컷오버 직전까지

★**남은 일은 [00-아침에-하실일.md](00-아침에-하실일.md) 하나에 모아 뒀습니다.**

```
✅ SNS 로그인 3사        kakao·naver·google ★셋 다 켬 · 금고 시크릿 8 → ★11건
   🚨그 과정에서 ★백엔드 전멸 직전을 막음 — 금고 목록 API 가 ★10건만 주는데
     로더가 첫 장만 읽어, 11건이 되는 순간 portone 키가 「없는 것」이 됐음
     ★"8+3=11인데 왜 10이지" 에서 멈춘 것이 잡았습니다

✅ ArgoCD prune 켬       ★"켜면 Secret 4개 날아간다"가 ★시험 안 해 본 틀린 가정이었음
                         버려도 되는 NS 에서 ★대조군을 두고 확인 → 지워진 것 ★0건
                         ⚠️Namespace 만 Prune=false · selfHeal 은 ★계속 끔

✅ 시크릿 접근 제어        ★안 하기로 결정 (근거 있음)
                         값은 이미 ★KMS 키(kms11) 명단이 막고 있었습니다
                         catchap-ops 는 금고 매니저인데도 ★403 KmsKeyAccessDenied

✅ 클라이언트 IP          ★LB · ingress · 앱 3층을 다 맞춤
                         전: 모든 사용자가 한 사람 (캡차 2주치 2,996건에 IP ★5개)
                         후: ★진짜 공인 IP · IP 로 묶인 제한 ★9개가 사용자별로
                         ★위조 헤더를 실제로 보내 ★막히는 것까지 확인

✅ 문서                  심사위원용·공부용·포트폴리오용 최신화 (0808 저녁)

✅ External Secrets      ★안 씁니다 — 0808 에 관문이 닫힌 것을 확인했습니다
                         (카카오 금고는 API·인증·엔드포인트가 AWS 와 다름)
                         → ★0810 에 앱 로더 이식으로 끝냈습니다. 클러스터 비밀값 ★7개
⬜ ★컷오버               DNS · DB 재이전 · 미디어 증분 · ★그리고 옛 서버 내리기
```

---

## ✅★★★0807 — GitHub 재설계 끝 · ArgoCD 붙임

★**아침에 하실 일은 [00-아침에-하실일.md](00-아침에-하실일.md) 하나에 모아 뒀습니다.**

```
✅ 저장소 6개 · 작업 저장소 브랜치 41 → ★0 (다섯 곳 전부 main 하나뿐)
✅ 병합 설정 6곳 통일 (squash only · 병합 후 자동 삭제)
✅ main 직접 push 감시 6곳 — ★만들자마자 제 push 를 잡음
✅ ArgoCD v3.5.0 · 앱 4개 ★Synced·Healthy · 파드 8개 Running·재시작 0
✅ ★자동 동기화(automated) 켬 — prune·selfHeal 은 끔
   ★0808 정정 — prune 을 끈 이유였던 "Secret 4개가 날아간다"는 ★틀린 가정이었고,
     시험으로 확인한 뒤 ★prune 을 켰습니다. selfHeal 만 계속 꺼 둡니다.
✅ 이미지 빌드·배포 워크플로 — ★한 바퀴가 사람 손 없이 돕니다
✅ ★CI — ★시험이 있는 3저장소 전부 초록
✅ ★백엔드 비밀값을 금고로 (0807 밤) — backend-secret 12개 → ★2개
⬜ ★SNS 로그인 6개를 금고에 — ★컷오버 차단 사항  → ✅0808 에 끝냈습니다 (위 0808 절)
✅ External Secrets — ★안 씀(관문 닫힘). ★0810 로더 이식으로 끝. 남은 값 ★7개
⬜ ★컷오버
```

★**0807 밤 갱신 — 위 ⚠️ 두 줄이 해소됐습니다.** 사람 이름 브랜치(`ms`)는
step-up 재적용 뒤 정리했고(PR #6), PR 대기이던 워크플로는 전부 병합됐습니다.
`sw-captcha` 는 고유 커밋 0이라 함께 정리했습니다.

★**CI 최종 (0807 밤 · 캡처 `40-깃허브캡처/0807-18~20`)**

```
catchap-backend    #202 ✅6m52s · #201 ✅6m25s · #200 ✅6m55s
                   #198 ✅8m17s · #197 ✅6m54s · #196 ✅7m17s
catchap-frontend   #278 ✅42s · #277 ✅39s · #275 ✅46s
catchap-infra      #4  ✅11s
behavior-ai · captcha · legacy   main-guard #1 만 (★제 직접 push 를 잡은 정당한 실패)
```

⚠️★**남은 빨간불은 손대면 안 됩니다** — `#199`·`#276` 은 **구버전 실행**(뒤 커밋이 대체),
`main-guard #1` 6건은 **감시 장치가 일한 기록**입니다.

⚠️★★**병렬화 정정 — CI 에서는 효과가 없습니다.**

```
로컬 32코어   28분 05초 → 1분 56초   ★14.5배
★CI          -n auto 6m52s  vs  비적용 6m25s~8m17s   → ★편차에 묻힘
```

★**왜로 보는 것**(⬜확인 못 함) — 러너 코어가 적고, **워커마다 앱 전체를 import**
해야 해서 시작 비용이 이득을 잡아먹는 것으로 봅니다. ⚠️**로그 본문을 못 읽어
실제 워커 수는 확인하지 못했습니다** — 판단 근거는 ★소요 시간 비교뿐입니다. **제가 「CI 도 2~3배」라고 문서 4곳에 적었던 것을
실측으로 전부 고쳤습니다.** 교훈은 **「원리가 맞다」와 「이 환경에서 통한다」는 다르다.**

⬜**작은 숙제** — CI 경고에 `Node.js 20 is deprecated`(`checkout@v4`·`setup-python@v5`·
`setup-node@v4`). **지금 고장난 건 아니고** GitHub 이 Node 24 로 올려 돌립니다.
PR 9개가 병합된 뒤 한 번에 판올림하는 게 낫습니다.

⚠️★**브랜치 보호를 못 겁니다** — Free 플랜 + 비공개 저장소라 강제되지 않습니다
(`won't be enforced on this private repository`). **유료 전환 안 함(사장님 결정)**.
그래서 `main` 직접 push 금지는 **약속**이고, `main-guard.yml` 이 **감지**만 합니다.

⚠️★**첫 Sync 직전에 막은 것** — `20-secret.예시.yaml` 이 앱 경로 안에 있어,
Sync 했으면 **살아 있는 `backend-secret`(항목 10개)이 `"여기에-넣지-말-것"` 2개로**
갈아치워졌을 것입니다. **누르기 전에 「무엇이 바뀌나」를 목록으로 확인**해서 잡았습니다.

⚠️★**옛 GPU 가 프로덕션 캡차·행동AI 를 돌리고 있었습니다** — 개인 작업 폴더에서.
`:8010` 행동 AI 는 **SSH 세션 스코프라 재부팅하면 안 옵니다**(sw님께 알릴 것).
★다행히 새 환경에 둘 다 이미 올라가 있어 **컷오버 전제조건은 충족**입니다.

★**공부·포트폴리오용 문서**를 새로 썼습니다 —
[91-팀공유/10-CICD-처음부터-끝까지-공부용.md](91-팀공유/10-CICD-처음부터-끝까지-공부용.md)

⚠️★**오늘 제가 틀린 것 다섯 가지**는 `30-작업이력/0807-인프라-작업기록-상세.md` 5절에 있습니다.
핵심은 하나 — **「측정이 안 된 것」을 「결과가 없는 것」으로 읽지 말 것.**

---

## ✅★★★0806 — 경보를 붙였습니다. ★실제 메일 도착까지 확인

**그전까지의 문제** — 재고·쌓고·그리는 것은 다 됐는데 **알리는 것이 없었습니다.**
Alertmanager 가 판단은 하는데 **받는 곳이 `null`** 이라 전부 버려지고 있었습니다.

### 카카오 Alert Center 를 먼저 알아봤습니다 — ★반쪽만 됩니다

```
받는 곳      이메일 · SMS · ★카카오톡 알림톡 · 슬랙 · 웹훅 · Pub/Sub
★지표 경보    VM · 로드밸런서 · MemStore · MySQL · PostgreSQL · Pub/Sub
             ⚠️★쿠버네티스 엔진이 ★없습니다
★이벤트 경보  쿠버네티스 있음 — 단 "클러스터가 생성·삭제됐다" 같은 ★서비스 사건이지
             "파드가 죽었다"가 아닙니다
```

★**그래서 나눠 씁니다.** 관리형(MySQL·MemStore·LB·VM)은 ⬜Alert Center 로 **사장님 콘솔 작업**,
클러스터 안(파드·노드·컨테이너)은 우리 Alertmanager 로 — 이번에 붙인 것이 이쪽입니다.

### 규칙 — 기본 120종이 이미 있고, ★못 잡는 것만 7개 더했습니다

`k8s/74-경보규칙-catchap.yaml`

```
CatchapPodMemoryNearLimit      제한 대비 85% 10분   ★OOMKill 은 예고 없이 옵니다
CatchapPodMemoryCritical       제한 대비 95%  5분
CatchapServiceDown             0벌  3분           (기본 규칙은 warning·15분이라 따로)
CatchapServiceSingleReplica    1벌 15분           ★이중화가 깨진 상태
CatchapPodRestartingRepeatedly 1시간 3회 초과      기본 CrashLooping 은 ★빠른 재시작만 잡음
CatchapImagePullFailing        이미지 못 받음 5분   배포 사고에서 제일 흔함
CatchapMetricsCollectionStopped 수집 끊김 10분     ★이게 끊기면 다른 경보도 같이 조용해짐
```

★**「안 울린다」를 「제대로 만들었다」로 읽지 않았습니다.** 문턱만 낮춘 **변이 시험**으로
몸통이 맞는지 확인했고(4개 즉시 확인·대조군은 안 물음), 나머지는 **일부러 장애를 내서**
확인했습니다 — 없는 이미지로 파드를 띄우니 `ImagePullBackOff` 를 정확히 잡았습니다.

⚠️`kube_pod_container_status_last_terminated_reason` 으로 OOM 을 잡으려던 규칙은
**뺐습니다** — 클러스터 전체에 **0계열**입니다(kube-state-metrics 가 안 내줍니다).
없는 지표 위에 규칙을 세우면 **영원히 조용한 경보**가 됩니다.

### 전달 — ★두 갈래로

```
①  Alertmanager ──웹훅──▶ ★우리 백엔드 ──▶ 운영자 전원 (콘솔 벨 + 메일)
    ★운영자 명단을 따라갑니다. Alertmanager 는 K8s 파일이라 DB 를 못 읽습니다.
②  급함(critical) 만 ── Alertmanager 가 ★직접 메일도
    ①은 백엔드가 죽으면 같이 죽습니다. 그런데 그게 제일 알아야 할 경보입니다.
```

⚠️②의 받는 주소는 **한 사람뿐**입니다(`09.daviya.09@daviya.kr`). Alertmanager 는 DB 를
못 읽어 명단을 따라갈 수 없습니다. **정식 통보는 ①이고 ②는 안전망**입니다.

★**Alertmanager 웹훅은 임의 헤더를 못 넣습니다**(`http_config` 가 basic_auth·authorization·
oauth2 만 지원). 그래서 백엔드가 `X-Metrics-Token` 과 **`Authorization: Bearer`** 를 둘 다
받게 했습니다. 이게 없으면 **손으로 시험할 땐 되는데 실제로는 403** 이 되는, 배포 후에야
드러나는 구멍이 됩니다.

### ★★끝까지 통했습니다 (실측)

```
14:36  일부러 만든 장애 → 프로메테우스가 경보 판단
14:37  백엔드 로그  "시스템 경보 수신: [급함] CatchapImagePullFailing (알림 5명)"
       ★EmailLog 10건 전부 sent — 급함 5통 + 경고 5통
14:42  "[해제] …"  ★해제는 벨만, 메일 안 감 (설계대로)
정리   시험 파드 제거 → Alertmanager 에 남은 것은 감시용 2건(둘 다 null 행)
       최근 5분 발송 ★0건
```

⚠️★**실제 운영자 5분께 메일이 나갔습니다** — 시험이었다고 알려 주셔야 합니다.

```
ww***@gmail.com · le***@naver.com · be***@naver.com · 09***@daviya.kr · mi***@naver.com
(vl***@naver.com 은 disabled 라 ★제외됐습니다 — 의도대로)
```

### 덜 보내는 장치 셋 — ★다 막아 버리지 않게

```
중복 제거   같은 제목 10분 내 재전송은 건너뜀 (Alertmanager 는 2xx 아니면 재전송)
등급        참고(info)·해제는 ★벨만 · 메일 안 감
상한        수신자 30명 초과면 ★아무에게도 안 보냄
            (일부만 보내면 "나는 받았으니 다들 받았겠지"가 되어 더 나쁩니다)
수신 설정   사람마다 끌 수 있음. 끄면 ★벨에는 계속 쌓이고 메일만 안 옴
            ★기본은 받음 — 안 받는 게 기본이면 아무도 설정을 안 만져 조용해집니다
```

### ★표를 새로 만들지 않았습니다

콘솔 벨(`Notification`)이 이미 있고 조회 API 도 있어서 그대로 씁니다.
**새 표는 마이그레이션을 부르는데, 이 저장소는 alembic head 가 3개이고
프로덕션 DB 는 그보다 뒤에 있습니다**(적용 판본 `captcha_purge_01`).
없어도 되는 위험은 만들지 않았습니다.

```
백엔드   f87c60d  시험 16개 · ★인증 관문 무력화 변이로 시험 2개가 무는 것 확인
이미지   catchap-backend:f87c60d  배포 완료 · 기존 기능 200
```

### ⚠️운영 콘솔 화면 — 만들었지만 ★배포는 못 합니다

`OpsAlerts.tsx`(429줄)를 만들어 타입검사·빌드·커밋까지 했습니다(`532799e`).
경보 목록·등급 색·안읽음 거르기·본문 보기·**메일 수신 켜고 끄기**가 들어 있습니다.

★**그런데 우리 가지에서 구우면 안 됩니다.** 배포된 콘솔에 **우리 가지에 없는 화면이 13개**
있습니다 — 팀원이 서버에서만 작업한 것들입니다.

```
OpsCourses · OpsHome · OpsInquiry · OpsInstructorProfile · OpsLearningAnalytics
OpsQuestionReview · OpsRenewalShared · ★OpsSystemStatus
Checkout · CourseDetail · FindIdPage · MyCourses · CertificateModal
```

⬜**프론트 소스를 가진 팀원에게 넘겨야 합니다.** 넘길 파일은 5개입니다.

```
새 파일   src/pages/ops/OpsAlerts.tsx · OpsAlerts.css
한 줄씩   src/routes/paths.ts  (OPS_ALERTS)
          src/routes/index.tsx (lazy import + Route)
          src/components/ops/OpsNav.tsx (메뉴 항목)
```

★배포된 콘솔에 이미 **`OpsSystemStatus`** 가 있습니다 — 겹치는지 팀원과 확인이 필요합니다.

---

## ✅★★0806 — 그라파나 정리: 「CatChap 한눈에」를 시작 화면으로

**문제였던 것** — 대시보드가 **25개**나 깔려 있어 정작 볼 화면을 매번 찾아 들어가야 했고,
관리형 지표는 **2조 자원까지 같이** 나왔습니다.

```
✅시작 화면      그라파나를 열면 ★「CatChap 한눈에」가 바로 뜹니다
                 실측: /api/dashboards/home → 제목 "CatChap 한눈에" · 패널 14개
✅우리 것만       모든 조회에 우리 자원을 고르는 조건이 들어 있습니다
                 실측: LB 정상대상 — 조건 있음 ★2계열 / 조건 없음 3계열(남의 것 포함)
```

**한 화면에 담은 것 14개**

```
서비스   떠 있는 벌 수(4계열) · 최근 1시간 재시작 · ★파드 메모리 대 ★제한(8계열)
노드     CPU · 메모리 · 디스크 (2대)
DB       ★대기 서버 지연(초) · CPU 주/대기 · 연결 사용률 · 디스크
캐시·입구 MemStore CPU·메모리 · 캐시 적중률 · ★LB 정상 대상(우리 것만)
안내      색의 뜻 · 여기 없는 것 · 값이 안 보일 때
```

★**우리 것을 고르는 조건** — 이걸 빼면 남의 자원이 섞입니다.

```
lb_name=~"catchap-.*"                    (2조는 LoadBalancer02·03)
instance_group_name="mysql-team1"        ★replication_role 로 주/대기 구분
cluster_name="catchap-valkey-prod"
```

★**지표 이름은 실제로 받아 보고 골랐습니다.** 문서에 있는 이름이라도 0계열인 것이 있었습니다.

```
✗ mysql_threads_connected  ✗ mysql_slow_queries  ✗ mysql_queries  ✗ disk_used_percent
✅mysql_connections_count  ✅mysql_slow_query_count  ✅mysql_query_persec
✅mysql_replication_lag ★대기 서버 지연 = 이중화 건강 지표 (지금 0.00)
✅mysql_connection_usage_percent  ✅mysql_defaultstorage_disk_used_percent
✅memstore_keyspace_hitrate_percent  ✅lb_healthy_host_count
```

**어떻게 넣었나** — 화면에서 손으로 만들지 않고 **매니페스트로 선언**했습니다.

```
k8s/73-그라파나-대시보드-catchap.yaml   ConfigMap · 딱지 grafana_dashboard: "1"
                                        → 사이드카가 주워 ★그라파나 재시작 없이 뜹니다
k8s/71-…values.yaml 에 2줄 추가          default_home_dashboard_path · default_timezone
                                        → helm upgrade (개정 3)
```

★**JSON 은 손으로 안 짰습니다** — 생성기(scratchpad `make_dashboard.py`)로 만들고
파싱으로 검증했습니다. 쉼표 하나 틀리면 화면이 통째로 안 뜹니다.

### ⚠️여기서 드러난 것 2건

```
① ★VM2 에 helm 이 없었습니다
   /tmp 에 풀어 쓰고 지웠던 모양입니다 — /tmp/helm.tgz 만 남아 있었습니다
   → ★/usr/local/bin/helm 으로 제대로 설치했습니다 (v3.16.3)
   ★교훈 — 다음 사람이 helm upgrade 를 못 하면 클러스터를 못 고칩니다

② ★덮어쓰기 전에 대조했습니다
   helm get values 로 ★클러스터에 적용된 값을 받아 로컬 파일과 비교
   → 차이가 ★제가 더한 2줄뿐인 것을 확인하고 올렸습니다
   (다른 사람이 화면에서 바꾼 게 있었다면 여기서 잡혔을 것입니다)
```

⚠️**남은 잡음** — `nodes-aix`·`nodes-darwin`(AIX·맥용)·`multicluster` 처럼
우리 환경에서 값이 안 나오는 기본 대시보드가 섞여 있습니다.
**차트에 개별로 끄는 설정이 없어** 지우면 다음 `helm upgrade` 때 되살아납니다.
시작 화면이 우리 것으로 바뀌었으니 **그대로 두었습니다.**

⚠️**GPU 는 이 화면에 없습니다** — 프로메테우스가 클러스터 밖을 못 걷어가서입니다.
운영 콘솔(`/ops/monitoring`)에서 봅니다. 화면 안 안내에도 적어 뒀습니다.

---

## ✅★★0806 — 그라파나에 카카오 지표 4개를 붙였습니다

```
데이터소스 (매니페스트에 선언 — 화면에서 손으로 안 넣었습니다)
  KakaoCloud-MySQL         uid kc-mysql      service-type=mysql
  KakaoCloud-MemStore      uid kc-memstore   service-type=memstore
  KakaoCloud-LoadBalancer  uid kc-lb         service-type=lb
  KakaoCloud-VM            uid kc-vm         service-type=server

★그라파나를 통해 실제 값 확인 (0806)
  kc-mysql     관리형 MySQL CPU   2계열  [8.29, 4.97]
  kc-memstore  MemStore CPU      2계열  [1.4, 1.0]
  kc-lb        LB 정상 대상       3계열  [0.0, 1.0, 1.0]
  kc-vm        VM 크레딧         20계열
```

★**자격증명은 K8s 시크릿 `kakaocloud-metric-export`** 에 두고 환경변수로 주입합니다.
**왜 금고가 아니라 K8s 시크릿인지**는 `k8s/71-…values.yaml` 주석에 이유와
**되돌릴 조건**까지 적어 뒀습니다(조회 전용 키 2줄이라 금고를 거쳐도 줄어드는 게 없음).

### ⛔★★0806 — GPU 지표는 ★막혔습니다 (카카오 에이전트 쪽 문제)

`catchap-monitor` 키가 나와서 GPU 서버에 카카오 모니터링 에이전트를 깔았는데,
**지표가 카카오로 안 들어갑니다.**

```
설치        kic-monitor-agent 1.0.0-0 (.deb · 7.7MB) — 설치는 정상
수집        Loaded inputs: cpu disk diskio mem net netstat ★nvidia_smi swap system
            nvidia-smi 를 에이전트 계정으로 실행 → Tesla T4 ★읽힘
전송        ★매번 실패
   E! response code returned 400, ★"sample timestamp out of order"
결과        service-type=server 지표 ★2종 그대로 (설치 전과 같음)
            우리 GPU(uuid 72e90c80…) 값 ★0계열
```

★**시험한 가설 셋 — 전부 아니었습니다.**

```
① 인증 문제?        ✗ 오류가 401/403 이 아니라 ★400. 자격증명 칸은 비어 있는데도
                      "Successfully connected" 가 나옴
② 빈 로그 경로?     ✗ KIC_LOG_FILE_PATH 가 비어 [[inputs.tail]] 이 files=[""] 를 읽고 있었음
                      → 껐는데 ★같은 오류
③ 한 묶음에 여러 시점? ✗ interval 10s→60s · flush_interval 60s 로 맞춤 → ★같은 오류
```

★**우리 쪽 원인이 아닌 근거**

```
서버 시계    NTP 동기화됨 · 오차 ★-2.686ms
계열 충돌    우리 GPU uuid 는 기존 20계열 어디에도 ★없음 (새 계열인데 "순서 어긋남")
```

⬜**멈춰 뒀습니다.** 설치는 남기고 서비스만 껐습니다(오류 로그가 60초마다 쌓여서).

```
지금        systemctl is-active → inactive · 부팅 시 disabled
원본 설정    /etc/kic_monitor_agent/kic_monitor_agent.conf.orig-0806
다시 켜기    sudo systemctl enable --now kic_monitor_agent
STT 워커     ★영향 없음 — 8100/health 200 · 컨테이너 Up 6 days
```

★**카카오는 GPU 지표를 「지원합니다」 — 문서로 확인했습니다.** 기능이 없어서가 아닙니다.

```
VM 지표 문서에 GPU 전용 절이 있습니다 ("GPU가 장착된 서버에서만 수집")
  nvidia_smi_utilization_gpu   nvidia_smi_memory_used    nvidia_smi_memory_free
  nvidia_smi_memory_total      nvidia_smi_power_draw
  https://docs.kakaocloud.com/en/service/management/monitoring/monitoring-metric

⚠️단, ★에이전트 설치 문서에는 GPU 이야기가 한 줄도 없고, 어떤 역할이 필요한지도 없습니다
⚠️쿠버네티스 GPU 는 카카오도 ★dcgm-exporter + 프로메테우스를 권합니다(자체 스택)
   https://docs.kakaocloud.com/en/tutorial/observability/nvidia-gpu-monitoring
⚠️GPU 전용 상품 ★AI Insight 가 따로 있습니다 — 별도 서비스입니다
```

⬜**(가)를 더 파려면** 카카오에 문의하거나 강사님께 여쭤야 합니다 —
**그들의 에이전트가 그들의 기본 설정으로 그들의 창구에 400 을 받습니다.**

### ✅★★0806 — GPU 지표를 ★우리 에이전트로 띄웠습니다 (운영 콘솔)

```
★새 클러스터 운영 콘솔 실측 (0806 12:4x)
  gpu-stt   GPU STT 워커   cpu 0.0%  mem 2.7%  disk 24.7%   28초 전
            ★GPU 0.0% · VRAM 3895/15360MiB · Tesla T4
```

**무엇을 깔았나** — `scripts/metrics_agent.py`(우리 저장소에 이미 있던 것)를 GPU 서버에.

```
/opt/catchap/metrics_agent.py          3,870바이트 · 옮긴 뒤 ★문법 검사로 안 깨진 것 확인
/etc/catchap-metrics-agent.env         600 root:root · ★토큰 48자
/etc/systemd/system/catchap-metrics-agent.service
   --loop 30 · Restart=always · ★DynamicUser=yes · ProtectSystem=strict · NoNewPrivileges
   Environment=PYTHONUNBUFFERED=1   ← ★없으면 로그가 버퍼에 갇혀 "안 도는 것처럼" 보임
python3-psutil                          apt 로 (우분투 24.04 는 pip 를 막아 둠)
```

★**토큰은 화면에 한 번도 안 나왔습니다.** K8s 시크릿에서 꺼내 GPU 서버까지
**두 ssh 사이의 파이프로만** 흘렸습니다(명령줄 인자로 주면 프로세스 목록에 보입니다).
받는 쪽 스크립트는 **길이만** 찍습니다.

### ★★여기서 걸린 함정 — 「보내진다」와 「도착한다」는 다르다

에이전트는 처음부터 `sent gpu-stt ...` 를 30초마다 찍었습니다. **그런데 새 콘솔의
`gpu-stt` 는 3일 전 값 그대로였습니다.**

```
원인   api.catchap5.com 은 ★아직 옛 서버(210.109.52.124)를 가리킵니다 — 컷오버 전이니 당연
결과   새 GPU 값이 ★라이브(옛) 콘솔에 덮이고 있었습니다 → 발견 즉시 멈췄습니다
```

★**교훈 — 에이전트가 2xx 를 받았다는 것은 「어딘가가 받았다」는 뜻이지
「내가 의도한 곳이 받았다」는 뜻이 아닙니다.** 반대편에서 확인해야 압니다.

**길 찾기 — 실측**

```
GPU → 워커노드 10.0.2.128:80        ★닫힘
GPU → VM2      10.0.2.165:22        ★닫힘   (gpu-2a 는 core-2a 로부터 완전히 격리)
GPU → 새 LB    210.109.55.233:443   ★★열림 200   ← 유일하게 열린 길
```

그래서 **보안그룹을 안 바꾸고** `/etc/hosts` 한 줄로 해결했습니다.

```
210.109.55.233  api.catchap5.com     ← 이 서버에서만. 백업 /etc/hosts.bak-0806
```

⚠️**기록을 잘못 읽어 한 번 헛짚었습니다** — `210.109.82.212` 를 우리 LB 로 봤는데
**2조 것**이었습니다. 우리 것은 `210.109.55.233`(2-a)·`61.109.239.102`(2-b) 입니다.

★**안전한지 먼저 확인하고 넣었습니다** — 이 서버가 백엔드를 부르는 다른 통신이 없어야
합니다. `stt-worker` 환경변수에 catchap/http/url/api **0건**, 바깥으로 열린 연결 **0건**,
백엔드가 8100 을 부르는 **단방향** 구조임을 보고 넣었습니다.
인증서는 `*.catchap5.com` 와일드카드라 **검증을 끄지 않고** 그대로 삽니다.

### ★적대적 자가검토에서 나온 것

```
✅ 재부팅 생존       systemctl is-enabled → enabled · /etc/hosts 는 파일이라 남음
✅ 화면 경로까지     _row_out() 을 직접 돌려 gpu_present=True·VRAM·경보[] 까지 확인
                    (DB 행만 보고 판단하지 않음)
✅ 신선도 여유       STALE_AFTER_SEC=120초 vs 전송 30초 — 4배 여유
✅ 라이브 콘솔 복구   옛 GPU 에도 ★같은 에이전트가 30초마다 돌고 있어 우리가 덮은 8분은
                    이미 되덮였습니다 (catchap-metrics.service · pid 259012)
★★컷오버 때 충돌     발견 → 92-킵-대기/02-콘솔작업 ⑤ 에 조치 2건 기록
```

⚠️**남는 한계 하나** — `/etc/hosts` 가 `210.109.55.233`(2-a)에 **못 박혀** 있어,
건물 A 의 LB 가 죽으면 **지표만** 끊깁니다(서비스는 고가용성 그룹이 살립니다).
컷오버 때 그 줄을 지우면 없어지는 한계입니다.

⚠️**한 번 헛짚은 것** — 옛 GPU 안에서 `127.0.0.1:8100` 이 무응답이라 옛 STT 가
죽은 줄 알았는데, **밖에서 보면 200 입니다**(컨테이너가 루프백에 안 붙어 있을 뿐).

---

## ✅★★★0806 — catchap-monitor 계정 생성 + Metric Export 연결 확인

**콘솔 작업(사장님) → 검증(제가) 순서로 진행했습니다.**

```
서비스 계정   catchap-monitor-kc-sfacspace05@kc.serviceaccount.com
계정 ID       d01c8ec3beaf4da185635acc455d0c5c
프로젝트 역할  ★프로젝트 리더 (Reader) — 이것 하나뿐
IAM 키       catchap-monitor-key · ID 11fea99ba92a4f9092f65a262694f6dd · 만료 무제한
             설명 "CatChap 지표 조회용"  (기존 규칙 `CatChap 운영·배포용` 과 같은 형식)
값 보관       인프라-캡처-비공개/자격증명.txt  ## 1-5 (백업 .bak-0806)
```

★★**403 이 사라졌습니다. `Reader` 하나로 충분했습니다.**

```
전  catchap-backend 키  403 {"error":"failed to get role (reader) in x-kep-roles"}
후  catchap-monitor 키  ★ke·mysql·memstore·server·lb ★다섯 다 200
지표  ke 25종 · mysql 62종 · memstore 86종 · server 2종 · lb 6종
```

★**실제 값까지 받아 봤습니다** — 라벨 목록이 온다고 데이터가 있는 것은 아니라서.

```
관리형 MySQL   CPU 6.82% / 5.27%   메모리 31.34% / 29.80%   ← 주·예비 두 대
MemStore      CPU 1.25% / 1.05%                            ← 두 노드
로드밸런서      아래 참조
```

### ⚠️★로드밸런서 지표에서 나온 것 두 가지

```
1943d4a5…   정상 0 · 비정상 3   ★LoadBalancer02 = ★2조 것입니다 (우리 게 아님)
7bf5cbc1…   정상 1 · ★비정상 -1  catchap-lb-2a
5d5117c7…   정상 1 · 비정상 0    catchap-lb-2b
```

★**`lb_unhealthy_host_count` 가 ★음수(-1)를 줍니다.** 대수가 음수일 수는 없습니다.
같은 시각에 **인터넷에서 `210.109.55.233`(2a) 로 4개 경로 전부 200** 을 받았으므로
**서비스는 정상이고 지표 쪽이 이상한 것**입니다.

⚠️★**경보를 만들 때 주의해야 합니다.**

```
`lb_unhealthy_host_count > 0`      → -1 은 안 걸립니다. ★쓸 수 있습니다
`lb_unhealthy_host_count != 0`     → ★-1 에 걸려 헛경보가 납니다
`lb_healthy_host_count < 1`        → ★2조 LB(정상 0)에도 걸립니다 — 우리 것만 걸러야 합니다
```

★**Metric Export 는 프로젝트 전체를 줍니다** — 2조 자원까지 보입니다.
경보·대시보드를 만들 때 **`lb_id` 로 우리 것만 골라야** 합니다.

---

## ★★0806 기록 전수 점검 — 문서와 실제를 대조했습니다

사장님 지시로 문서에 적힌 사실을 **살아 있는 인프라와 하나씩 맞춰 봤습니다.**

### 낡아서 고친 것 6건

```
1 로드밸런서 표         "지금은 1대 · 죽으면 서비스 끊김"  ← 0804 에 고쳤는데 표가 안 고쳐졌음
                       → 고가용성 그룹 2대. ★양쪽 IP 를 따로 찔러 4경로 200 확인
2 ★lb-2b 의 Error 상태  "배포 뒤 Online 이 되는지가 ★컷오버 전제 조건" ← ★열려 있던 항목
                       → 0805 배포로 해소. ★닫혔습니다
3 백엔드 이미지 태그     79d5e2d05a1e → ★824e7c4 (커밋 해시)
4 VM1~VM4 상태         "아직 비어 있음 · docker 없음"
                       → VM1 점프 · ★VM2 빌드호스트(docker·kubectl·helm) · ★VM3·VM4 는 여전히 빔
                       ⚠️★제가 "역할 미정" 이라고 적은 것은 ★틀렸습니다 —
                         00-아키텍처-설계.md 2-G 에 "VM3=예비(스테이징 후보)·VM4=예비" 로
                         ★설계돼 있었습니다. 0730 메모만 보고 적은 것이 원인입니다
5 MemStore             "인증 비밀번호 불필요" + 옛 IP(10.0.6.58·10.0.2.54)
                       → 0803 재생성으로 ★인증·TLS 켜짐 · ★IP 도 바뀜(10.0.2.231·10.0.6.101)
                       ★옛 IP 둘 다 지금 닫혀 있습니다
6 catchap-ci 권한       "1순위 cluster-admin 좁히기" ← ★이미 좁혀져 있음
                       → ClusterRoleBinding 은 readonly 뿐. cluster-admin 바인딩 ★없음
```

### 맞았던 것 (그대로 둠)

```
DB 테이블 99개      ★98 실체 테이블 + 1 뷰 = 99. ★제가 세는 방법이 좁았던 것
HTTPS 인증서        notAfter ★Oct 19 2026 · 양쪽 LB 동일 · SAN *.catchap5.com
금고 시크릿 8건      ★ConfigMap 의 SECRETS_NAMES 8건과 ★정확히 일치
DB 계정 격리        catchap_backend 에게 catchap 만 보임 (captcha·ai 는 목록에도 없음)
오브젝트 스토리지    catchap-storage-prod-team1 접근 200 · 객체 있음
옛 서버            www.catchap5.com 이 여전히 옛 서버로 200 (컷오버 전이라 정상)
```

### ★★팀원 계정 점검 (0806) — 세 층이 서로 다릅니다

```
① 서버(리눅스)   ★없습니다 — 5대 전부 `ubuntu` 하나뿐 (실측)
                 SSH 키도 ★Team1-Key.pem 하나뿐 (주석 "Generated-by-Nova")
                 → ★모두가 같은 키를 씁니다. 누가 무엇을 했는지 구분이 안 됩니다
                 → ★키 하나가 유출되면 5대가 한꺼번에 열립니다
                 → 팀원은 서버에 못 들어갑니다

② 데이터베이스   ★기록에는 있습니다 — jy·sw·ms·my 4명
                 catchap·catchap_ai·catchap_captcha 에 ★SELECT 만
                 근거: 00-작업기록.md:2914 · 90-강사님보고/00-종합보고-0803.md:126
                       (0803 에 실제 접속으로 권한 경계를 확인했다고 적혀 있음)
                 ⚠️★지금 다시 확인하려면 DBA 계정이 필요합니다
                   앱 계정(catchap_backend)은 mysql.user 를 못 읽습니다
                   — 그건 앱 계정이 제대로 좁다는 뜻이라 정상입니다

③ 쿠버네티스     ★팀원 신원 없습니다
                 붙는 신원이 catchap-ci-kc-sfacspace05@kc.serviceaccount.com 하나
                 roles: [member reader load-balancer_member]

④ 카카오 콘솔    ⬜제가 확인 못 합니다 (사장님 화면)
```

★**앱 계정 권한을 확인한 김에 적어 둡니다 — 잘 좁혀져 있습니다.**

```
catchap_backend@%
  GRANT USAGE ON *.*
  GRANT SELECT, INSERT, UPDATE, DELETE ON `catchap`.*     ← ★DDL 없음 · 다른 DB 없음
```

⬜**VM3 를 개발 서버로 줄 때 ①을 같이 풀어야 합니다.** 지금처럼 공용 키 하나로 두면
개발 서버에서도 누가 무엇을 했는지 모릅니다.

### ★점검하다 새로 드러난 것 2건

```
① ★VM3(10.0.2.212)·VM4(10.0.2.21) 가 ★6일째 아무것도 안 합니다
   앱은 전부 쿠버네티스로 갔고 빌드는 VM2 가 합니다
   ⬜쓸 곳을 정하거나 ★반납해서 비용을 줄여야 합니다
② VM2 의 3000 포트는 ★제가 그라파나를 보려고 연 임시 통로입니다
   그라파나 접근 방법을 정할 때 같이 정리합니다
```

★**교훈** — 「고쳤다」와 「문서에 반영했다」가 다릅니다. **6건 중 4건이 "고쳤는데 문서가
안 따라온" 경우**였습니다. 특히 2번은 **컷오버 전제 조건이 열린 채로 남아 있었습니다.**

---

## 0-0. ★★★지금 떠 있는 것 (0806 새벽 실측)

```
backend-api   catchap-backend:★824e7c4          ★진짜 커밋 해시 (git show 824e7c4)
frontend      catchap-frontend:488c549a0b53     이미지 ID — 어느 커밋인지 모름
behavior-ai   catchap-behavior-ai:feea9a597269  이미지 ID — 〃
captcha-api   catchap-captcha:d519323cdfe0      이미지 ID — 〃

전부 2벌 · 서로 다른 노드 · 재시작 0회
경로 8개 200 (양쪽 노드 · Host 헤더)
캡차→AI  reachable:true · model_loaded:true · policy_matches:true
```

★**backend 만 태그가 커밋 해시입니다.** 0806 에 모니터링 수집을 넣으면서 다시 구웠고,
그때 `origin/jy` 기준 커밋을 만들어 그 해시를 태그로 썼습니다.
**나머지 셋은 여전히 이미지 ID** 라 어느 코드인지 못 가리킵니다 — 팀이 다시 구울 때 정리합니다.

⚠️**0805 판에는 `79d5e2d05a1e` 로 적혀 있었습니다.** 그 이미지는 옛 서버의
`catchap-backend:latest` 를 그대로 가져온 것이었고, `824e7c4` 는 그보다 앞섭니다
(모니터링 수집 + jy님의 결제 수정 2건).

★**남은 것은 컷오버 하나입니다.**

### ⚠️오늘 배운 것 둘 — 다음에 또 걸립니다

```
① ★/health 응답이 같다고 코드가 같은 게 아니다
   behavior-ai 세 벌이 모델 이름·버전·스키마를 똑같이 보고했는데 ★파일 3개가 달랐다
   → 이전할 때는 ★파일 크기·지문을 대조한다

② ★"옛 서버 .env 에 있다" 와 "지금 코드가 읽는다" 는 다르다
   빠뜨렸다고 본 설정 6개 중 ★실제 문제는 0개였다
   (POSITION_TOLERANCE_PX·CHALLENGE_DIR 은 지금 config.py 에 없는 옛 잔재)
```

### ⬜남은 일 — 0806 새벽 실측 기준으로 다시 셌습니다

**사장님만 할 수 있는 것 (콘솔·GitHub)**

```
1  ★컷오버 (가비아 3~4줄)          전제조건 충족. 맨 나중
2  ★결제가 아직 안 켜집니다         PORTONE_STORE_ID · WEBHOOK_SECRET ★둘 다 빈 값(실측)
                                  포트원 콘솔에서 받아야 함
                                  (CHANNEL_KEY 48자·API_SECRET 80자는 이미 들어 있음)
⬜3 시크릿 접근 제어 8건            ★킵 (0806 결정) — 로더 병합 뒤로 미룹니다
                                  ★급하지 않습니다. 관문 세 겹 중 앞의 둘이 이미 막고 있습니다
                                    ① RBAC  → Reader(팀원·kdt5 15명·monitor) 탈락
                                    ② ★KMS  → catchap-ops·catchap-ci 탈락
                                    ③ ACL   → 지금 미사용. 켜면 세 번째 겹
                                  ★읽히는 것은 catchap-backend 하나뿐 (0806 실측)
                                  🚨★2026-08-10 — ★이 판단이 뒤집혔습니다.
                                    kms11 명단에 계정이 ★3개(backend·captcha·behavior-ai)가 되어
                                    ★캡차 열쇠로 백엔드 시크릿까지 읽힙니다(실제로 읽어 확인).
                                    ⬜켤 때 명단은 ★시크릿마다 그 앱 하나씩. 팀원은 넣지 않습니다
                                    → 92-킵-대기/13 에 안전한 순서를 적어 뒀습니다
✅4 ★catchap-monitor 계정(reader)   ★0806 완료 — 만들고 값 넣고 연결까지 확인
                                    ① Metric Export 조회 → 그라파나 4개 붙음 ✅
                                    ② GPU VM 카카오 에이전트 → ★400 으로 막힘
                                       → 우리 에이전트로 우회 ✅ (콘솔에 GPU 뜸)
5  GitHub 저장소·브랜치 재설계       설계 끝남 · 사장님과 같이
                                  → 92-킵-대기/06-깃허브-재설계.md
✅6 ★★DB 자동 백업 — ★켜져 있습니다 (0806 콘솔 확인. 아래 참조)
```

### ✅★★0806 — DB 자동 백업 확인 완료. 걱정했던 상황이 아니었습니다

제가 *"카카오 문서가 「자동 백업 옵션을 활성화한 후」라고 쓰니 안 켜져 있을 수 있다"* 고
적어 뒀는데, **콘솔에서 보니 켜져 있고 실제로 돌고 있었습니다.**

```
인스턴스 그룹  mysql-team1 · ✓Available · 고가용성 · ★다중 AZ 사용
              ★자동 백업 보존 ★14 일

자동 백업 목록  ★총 11건 — 7/27 만든 뒤 ★하루도 안 빠짐 · ★전부 Succeeded
  2026-08-05  ★0.68 GB   보존 만료 2026.08.20
  2026-08-04  ★0.68 GB
  2026-08-03  ★0.68 GB
  2026-08-02   0.19 GB
  2026-08-01 ~ 07-27  각 0.19 GB
수동 백업     0건 (손으로 만든 적 없음 — 정상)
```

★★**크기가 08-02 → 08-03 에 `0.19GB → 0.68GB` 로 뛴 것이 결정적입니다.**
그날이 **데이터 이전을 한 날**입니다. 즉 **백업이 빈 껍데기가 아니라 실제 데이터를 담고
있다**는 증거입니다. 「백업이 있다」가 아니라 「백업에 데이터가 들어 있다」까지 확인됐습니다.

⬜**보존 14일이 적당한지는 따로 판단할 것.** 1~35일 중에 고를 수 있습니다.
결제·학습 기록이 들어가면 더 길게 두는 편이 나을 수 있습니다.

### ★★0806 — DB 접속 주소의 구조를 확정했습니다 (옛 「남는 확인 항목」의 답)

`00-작업기록.md:1744` 에 **"장애 조치 시 엔드포인트·포트 동작(해석 A/B)"** 을 확인 항목으로
남겨 뒀는데, **오늘 재서 답이 나왔습니다. 해석 A 가 맞습니다.**

```
az-a:3306 → uuid ef47c0af…  ★주      az-b:3306 → uuid ef47c0af…  ★같은 주
az-a:3307 → uuid d541f877…  예비     az-b:3307 → uuid d541f877…  같은 예비

★포트가 역할   3306 = 언제나 지금의 주 · 3307 = 언제나 예비
★호스트가 입구  az-a = 건물 A 입구(10.0.2.193) · az-b = 건물 B 입구(10.0.6.76)
★건물 이름 없는 주소는 ★없습니다 (mysql-team1.… 은 안 풀림)
```

★**앱이 견디는 근거도 확인했습니다.**

```
pool_pre_ping=True · pool_recycle=3600   죽은 연결을 걸러냄
★DNS TTL = 1초                          카카오 문서가 "IP 가 바뀔 수 있으니 캐싱 말라" 는데
                                         TTL 1초라 구조적으로 안전
```

⚠️★**남는 위험 하나** — 앱은 `az-b`(건물 B 입구)만 봅니다. **건물 B 가 통째로 죽으면
그 입구도 같이 죽습니다.** 카카오가 그때 `az-b` 이름을 살아 있는 쪽으로 돌리는지는
**문서에 없고 확인 못 했습니다.**

```
안 돌려 준다면  ★사람이 접속 주소를 az-a 로 바꾸고 앱을 재시작 (몇 분)
```
★**"모른다"를 그대로 적어 둡니다.** "자동으로 됩니다" 라고 써 두면 그날 아무도 대비가 없습니다.

**제가 할 수 있는 것**

```
6  ★HPA (자동 확장)               metrics-server 를 깔아 전제 충족. ★지금 2벌 고정
                                  ⬜정할 것 — 최대 몇 벌 · 임계 몇 %
7  ★알림 받는 곳                   Alertmanager 에 수신자 ★0명. 임계를 넘겨도 아무 데도 안 감
                                  ⬜정할 것 — 메일(SMTP 있음)인가 메신저인가
8  그라파나 접근 방법               ClusterIP + port-forward. ★인증 붙이기 전엔 인터넷 미노출
9  ★GPU 지표                       ★4번 키가 나오면 카카오 에이전트를 제가 깝니다
                                  (자체 에이전트 불필요 — 0806 정정. 아래 참조)
10 ★볼륨 백업                      VolumeSnapshotClass ★없음
                                  프로메테우스 20GB·그라파나 5GB 가 백업 대상 밖
11 문서·보고                       0806 일일보고 · 강사님 보고
⬜ ArgoCD/GitOps                  ★컷오버 뒤에. 설계는 끝남 → 92-킵-대기/05
```

**팀원 몫 (지금 개발 중 — 건드리지 않습니다)**

```
12 캡차 ★sw-captcha(+21) ↔ ms(+17)  둘 다 0805 커밋. 저장소 나누기 전에 병합 필요
13 결제 시험 2개 갱신                 낡은 정책을 검사 중
14 jy님 로더 병합                    feat/media-object-storage → jy
```

### ★★0806 정정 — GPU 지표는 우리 에이전트가 아니라 카카오 에이전트입니다

0805 에 *"카카오가 VM 지표를 크레딧 2종만 주니 우리 에이전트 말고 길이 없다"* 고 적었는데
**틀렸습니다.** 원인은 **에이전트 미설치**였습니다.

```
카카오 문서   "nvidia_smi 지표는 ★GPU가 장착된 서버에서만 수집됩니다"
             "에이전트 설치 후 약 3분이면 데이터가 쌓이기 시작합니다"
우리 GPU 워커  ★유닛 없음 · 파일 없음 · psutil 없음 = 아예 설치된 적 없음

설치  wget …/kic-monitoring-agent/package/kic_monitor_agent_1.0.0_amd64.deb
      sudo dpkg -i kic_monitor_agent_1.0.0_amd64.deb
      ★설치할 때 액세스 키(ID·보안키)를 넣습니다
얻는 것  nvidia_smi_utilization_gpu · memory_used/free/total · power_draw
        + CPU·메모리·디스크·네트워크
```

★**이쪽이 낫습니다** — 카카오가 관리하고 **Metric Export 로 그라파나에 통합**됩니다.
관리형 MySQL·MemStore·LB 와 **같은 창구**가 됩니다.
⚠️에이전트 설치용 키에 **어떤 역할이 필요한지는 문서에 없습니다.** `reader` 로 안 되면 그때 확인.

### ★★0-1. 모니터링 — 컷오버하면 화면이 통째로 죽습니다 (0805 조사 완료)

```
지금 DB 실측   ai 2일 · backend ★8일 · db 2일 · frontend 2일 · gpu-stt 2일 전
              → ★5행 전부 「오래됨」(기준 120초). 에이전트가 새 환경엔 하나도 없음
```

★**깨지는 자리 다섯 — 전부 실측했습니다.**

```
① EXPECTED_SERVERS 가 옛 VM 이름 (db·gpu-stt·frontend)
② ★백엔드 self-collect 가 두 파드에서 같은 행을 덮어씀
   4mqnc cpu=2.5% / fw94z cpu=1.2%  ← 같은 순간 실측. 요청마다 튐
③ ★psutil 이 파드가 아니라 노드를 봄 (mem_total 15,993MB = 노드 용량)
④ ★self-collect 는 화면을 열어야만 갱신 (backend 행이 8일 전인 이유)
⑤ ★관리형 MySQL 은 SSH 가 안 돼 에이전트를 넣을 방법이 아예 없음
```

★**카카오가 대신 줍니다** — 그리고 **제가 처음에 틀리게 판단했습니다.**
07-29 캡처의 K8s 대시보드가 「노드 개수」만 그려서 *"K8s 는 그것만 준다"* 고 적었는데,
문서를 보니 **노드 CPU·메모리·디스크·네트워크 + 파드 지표까지 다 줍니다.**
기본 대시보드가 그것만 그린 것뿐입니다.

```
★kube-system 의 ke-prometheus-node-exporter 가 그 증거
  카카오 자체 레지스트리 이미지 · hostNetwork · 포트 ★59100 · 클러스터 생성 시각에 심어짐
  실측 59100/metrics → 855줄. node_cpu_seconds_total 등 문서의 지표 이름과 일치
★클러스터 안에 프로메테우스 서버는 없음 (ServiceMonitor CRD 조차 없음)
```

★**Metric Export 로 밖에서 당겨올 수 있습니다 — Prometheus HTTP API 호환.**

```
https://monitoring.kr-central-2.kakaocloud.com/metric-export/grafana/{프로젝트ID}/prometheus
헤더  Credential-ID · Credential-Secret · service-type(server|lb|ke|mysql|memstore|…)

실측  파드에서 443 연결 ★됨 · 무인증 400 = 도달 확정
★★★ 필요한 것은 ★「reader 역할」 하나로 확정됐습니다
     catchap-backend IAM 키 → 403 {"error":"failed to get role (reader) in x-kep-roles"}
     ★catchap-ci IAM 키(reader 보유) → ★200 · 다섯 service-type 전부
⚠️  ★QSS(질의 표본 수) 기반 과금 — 폴링 주기 60초 이상 권함
```

★**직접 받아 센 지표 수** — ke 25종(★파드 단위까지) · mysql 62 · memstore 86 · lb 6

```
★★server(VM) 만 2종뿐  cpu_credit_balance · cpu_credit_usage
   문서는 "에이전트 없이 CPU·메모리·디스크·GPU 를 준다" 고 하는데 ★실측이 다릅니다
   → ★GPU STT 워커는 카카오에서 못 가져옵니다. 우리 에이전트를 그대로 씁니다
   (어차피 GPU util·VRAM 은 nvidia-smi 로 우리가 이미 읽습니다)
```

⬜**사장님 콘솔 작업은 셋뿐입니다** — `catchap-monitor` 계정(역할 ★reader 하나) →
액세스 키 1개 → 금고에 시크릿 1건. ★역할은 **생성 후 수정 불가**라 만들 때 같이 주셔야 합니다.
★`catchap-backend` 에 안 붙이는 이유 = `reader` 는 **프로젝트 전체 읽기**라 앱 계정엔 넓습니다.

### ★★★사장님 결정 = (다). 그리고 ★올렸습니다 + 운영 콘솔까지 연결 (0805 밤)

```
✅ 블록 스토리지 CSI        cinder-csi · StorageClass 3개 (★이게 없어서 막혀 있었음)
✅ kube-prometheus-stack    88.1.5 · 네임스페이스 monitoring · 파드 7개 전부 Running
✅ metrics-server           v0.9.0 · ★kubectl top 됨 → HPA 를 쓸 수 있게 됨
✅ ★운영 콘솔도 같은 값     백엔드가 프로메테우스에서 걷어 server_metrics 에 씀
                            커밋 ★824e7c4 · 이미지 catchap-backend:824e7c4
⬜ 카카오 Metric Export     ★catchap-monitor 계정(reader) 대기 — 관리형 MySQL·MemStore·LB·GPU 용
```

★★**이미지 태그가 드디어 진짜 커밋 해시입니다.** 앞선 넷은 전부 이미지 ID 였습니다.
`git show 824e7c4` 로 정확히 그 코드를 봅니다.

★**덤으로 알게 된 것** — 프로덕션 이미지와 `origin/jy` 를 201개 파일로 대조했더니
**다른 것은 2개뿐**이었고 둘 다 **git 이 더 큽니다**(jy님의 오늘 결제 수정).
즉 git 이 뒤처진 게 아니라 **앞서 있었습니다.** 이번 배포에 그 수정도 같이 들어갔습니다.

⚠️**결제 시험 2개가 실패합니다 — 제 변경 탓이 아니고 ★시험이 낡았습니다.**
커밋 `bebca8e`(진행률 기반 비율 환불)가 `payments.py` 만 고치고 `tests/` 는 안 고쳤습니다.
시험은 여전히 옛 정책("시청 시작 = 무조건 환불 불가")을 검사합니다.
★새 정책은 payments.py 731-735 주석에 있습니다(사장님 결정 2026-08-05).
→ 별도 작업으로 올려 뒀습니다.

**검증 (실측)**

```
프로메테우스 타깃 19개   ★전부 up · down 0개      수집 지표 종류 ★1,957개
그라파나 13.1.2         /api/health {"database":"ok"} 200
★파드가 각각 보임       backend-api 216.2 / 215.1 MiB (ops 콘솔은 한 칸에서 뒤섞였음)
kubectl top nodes       122m 3% 2084Mi 13% · 75m 1% 1951Mi 12%
```

**★설치하며 잡은 함정 다섯**

```
① 카카오 공식 YAML 이 ★2022년 latest 이미지        → v1.33.0 고정 (클러스터와 같은 버전)
② ★Immediate + 다중 AZ = 볼륨과 파드가 다른 존     → WaitForFirstConsumer 클래스 추가
③ ★관리형이라 컨트롤 플레인 타깃을 꺼야 함          → 안 끄면 down 이 계속 뜸
④ ★apiserver 가 파드 네트워크에 못 닿음            → metrics-server 를 hostNetwork:4443 으로
   (힌트가 이미 있었음 — ingress-nginx 가 hostNetwork 인 이유가 같음)
⑤ ★전략적 병합이 포트 목록을 합쳐 이름 중복        → --type=json 정밀 교체
```

★문서 — `92-킵-대기/07-모니터링-재설계.md` · `k8s/70~72`

⚠️**0805 유출 1건** — 그라파나 admin 비밀번호가 화면에 찍혔습니다.
`-o custom-columns=…:.data` 를 썼는데 **`.data` 에는 값이 base64 로 들어 있습니다.**
★**즉시 재생성했고 그라파나 설치 전이라 그 값은 한 번도 쓰이지 않았습니다.**
★규칙 — 시크릿은 `jsonpath='{.data}'` 로 받아 **키 이름과 길이만** 출력합니다.

✅**새 GPU 워커를 확인했습니다** — 표의 `gpu-stt disk 82%` 는 **옛 서버** 값이었습니다.
새 워커(`10.0.5.57`)는 **디스크 25%** · Tesla T4 · `{"ok":true,"device":"cuda","loaded":true}` 정상.
⚠️다만 `catchap-metrics` 에이전트가 **inactive** 이고, 카카오 VM 지표가 크레딧 2종뿐이라
**GPU util·VRAM 을 얻으려면 이 에이전트를 켜야 합니다.**

### ★★★1번 결과 — RBAC 으로는 못 좁힙니다 (0805 21:xx)

```
가짜 사용자로 잰 시험    필요한 것 12개 yes · 못 해야 할 것 5개 ★no  ← 완벽
cluster-admin 삭제 후    ★막혀야 할 3개가 여전히 됩니다
                         kube-system 시크릿 · clusterrolebinding 생성 · default 조회
```

**원인** — `catchap-ci` 의 클러스터 신원에 IAM 역할이 실려 옵니다.

```
Extra: alpha.kubernetes.io/identity/roles = [★member reader load-balancer_member]
→ 「프로젝트 멤버」만으로 클러스터 전체가 열립니다. RBAC 을 좁혀도 그 위에서 통과합니다
```

★★**그리고 뗄 수 없습니다** — CR 에 push 하려면 프로젝트 멤버가 필요하고
(Container Registry 전용 역할이 없음), 멤버이면 클러스터가 열립니다.

★**그래서 (나) ArgoCD 가 유일한 실질적 해답입니다** — CD 자격증명을 아예 안 두는 것.

★**교훈** — 「권한을 줄였다」를 `auth can-i` 로만 확인하면 틀립니다.
그 도구는 **RBAC 만** 봅니다. **금지된 동작을 실제로 해 봐야** 다른 관문이 드러납니다.

---

## 0. ★★0805 16:59 — 금고 준비가 끝났습니다

```
시크릿 7건 → ★환경변수 9개 주입
  기존 5건   JWT · SMTP · STT · 메트릭 · 포트원(API_SECRET·CHANNEL_KEY)
  ★신규 2건  catchap-database-url · catchap-media-s3-keys

검증 (전부 0805 실측 · 값 미출력)
  DB      ★붙음  read_only=0 · db=catchap · 테이블 99개
  버킷     ★붙음  catchap-storage-prod-team1
  ENV=prod ★부팅 관문 통과 — JWT·CORS·SMTP 검사 다 지나감
```

### ★★★네 서비스가 다 떴습니다 (18:0x) — 컷오버 전제조건 충족

```
backend-api  2/2   behavior-ai  2/2   captcha-api  2/2   frontend  2/2
전부 서로 다른 노드 · 인그레스 셋(api·captcha·www) 적용

밖에서 오는 길 (Host 헤더 · 양쪽 노드 동일)
  www.catchap5.com /              200      catchap5.com /courses/3   308
  api.catchap5.com /api/v1/health 200      captcha.…/health/ready    200
  www.catchap5.com /courses/3     200      captcha.…/api/config      200

캡차→AI 연동   ★reachable:true · model_loaded:true · policy_matches:true
금고 시크릿     ★8건 → 환경변수 10개 (MAIN_CAPTCHA_SITE_SECRET 포함)
```

★**남은 것은 컷오버(가비아 3~4줄)뿐입니다.** 그건 사장님이 하십니다.

### ⚠️구동해 보고 찾은 것 2개 (0805)

```
① ★EMBED_ORIGINS 를 내가 빠뜨렸다 — 없으면 www 에서 캡차 위젯이 iframe 으로 안 뜬다
   넣고 확인: frame-ancestors 'self' https://www.catchap5.com https://catchap5.com
② CAPTCHA_SITE_KEY 를 새로 만든 게 프론트를 깨는지 → ★안 깬다
   빌드물에 site_ 값이 0건. 컴포넌트가 siteKey 를 속성으로 받는다
```

★**둘 다 스키마 검증으로는 안 나옵니다.** 띄워서 응답을 봐야 나옵니다.

### ⬜컷오버 뒤 순서 (0805 정리 → ★0806 점검으로 갱신)

```
✅1순위  catchap-ci 의 cluster-admin 좁히기   ★RBAC 은 좁혔습니다 (0806 실측)
        ClusterRoleBinding  catchap-ci-readonly → catchap-ci-readonly
        RoleBinding(catchap) catchap-ci-deployer → catchap-deployer
        ★cluster-admin 바인딩은 ★없습니다
        ⚠️다만 IAM 역할이 그 위에서 통과시킵니다 — 그래서 (나) ArgoCD 가 진짜 해답입니다
 2순위  캡차·behavior-ai 비밀값을 금고로 (기동 래퍼)   두 이미지 다 파이썬 3.12 있음
 3순위  시크릿 접근 제어 8건 켜기
 4순위  PORTONE_STORE_ID·WEBHOOK_SECRET 받아 결제 켜기 (★둘 다 지금 빈 값 — 0806 실측)
```

⬜**접근 제어는 생성 화면에서 켜면 만들어지지 않았습니다**(0805). 상세 화면의
`[접근 제어 사용]` 으로 나중에 켭니다 — 어차피 기존 5건도 컷오버 뒤에 켤 예정이라
**7건을 한 번에** 합니다. ★왜 생성 때 막히는지는 따로 확인할 것.

---

## 1. 지금 목표 한 줄

**백엔드·캡차를 쿠버네티스에 올린다.** 화면(프론트엔드)은 0804 에 떠서 동작 중이고,
`api.catchap5.com` 이 아직 **503** 이다(뒤에 앱이 없어서). 이것만 채우면 컷오버 조건이 갖춰진다.

---

## 2. ★비밀값을 어디에 두기로 했나 — 이미 정해져 있다

**근거** — [00-아키텍처-설계.md](00-아키텍처-설계.md) **2-E** · 코드 주석
`app/core/secrets_loader.py` 머리말

```
(가) ★채택   앱이 기동할 때 Secrets Manager 를 읽는다.  K8s Secret 에는 부트스트랩 키 1개만
(나) 버림    비밀값 13건을 K8s Secret 으로 복사한다
```

**(나)를 버린 이유 두 가지** — 코드 주석에 그대로 적혀 있다.

1. 비밀값을 바꿀 때 **쿠버네티스를 안 건드려도 된다.** 새 버전 만들고 파드만 재시작하면 끝.
   (나)였다면 **매번 매니페스트도 같이** 고쳐야 한다.
2. ★**K8s Secret 은 이름과 달리 암호화가 아니라 base64 다.** etcd 에 사실상 평문으로 들어가고
   클러스터를 볼 수 있는 사람은 다 읽는다. Secrets Manager 는 **kms11 로 암호화**되고
   **누가 언제 읽었는지 감사 기록**이 남는다.

★**무상태화와 같은 이야기다**(사장님 0805). 파드는 아무것도 안 들고 있어야 한다.
(나)는 **클러스터가 상태를 들게 만들고 두 곳을 맞추게** 한다. (가)는 **금고 한 곳**만 둔다.

⚠️**그러니 「값을 꺼내서 K8s Secret 에 넣는 작업」은 하지 않는다.** 이걸 또 하려고 하면
그건 벗어난 것이다.

---

## 3. ★코드가 이미 있다 — 이미지 안에서 확인함 (0805)

```
/app/app/core/secrets_loader.py            219줄
/app/app/core/config.py:7                  from app.core.secrets_loader import load_secrets_into_env
/app/app/core/config.py:233                load_secrets_into_env()   ← Settings() 만들기 직전
```

★**Settings 를 만들기 전에** 부른다. 비밀값이 Settings 의 재료라 순서가 반대면 의미가 없다.

### 환경변수 계약 (코드에서 직접 읽음)

| 이름 | 필수 | 설명 |
|---|---|---|
| `SECRETS_BACKEND` | ★ | `none`(기본) 또는 `kakaocloud`. **기본이 none 이라** 로컬·테스트는 영향 없음 |
| `SECRETS_ACCESS_KEY` | ★ | `catchap-backend` 서비스 계정 **액세스 키 ID** |
| `SECRETS_SECRET_KEY` | ★ | 같은 계정 **보안 액세스 키** |
| `SECRETS_NAMES` | ★ | 읽을 시크릿 이름을 쉼표로. **전부 긁지 않는다**(뒤 설명) |
| `SECRETS_IAM_ENDPOINT` | | 기본 `https://iam.kakaocloud.com/identity/v3` |
| `SECRETS_ENDPOINT` | | 기본 `https://secrets-manager-service.kr-central-2.kakaocloud.com` |

★**`SECRETS_NAMES` 를 필수로 둔 이유** — 프로젝트 시크릿을 전부 긁으면 남이 만든 것까지
읽으려 들고(접근 제어 걸린 건 실패), **나중에 시크릿이 하나 늘면 앱 동작이 말없이 바뀐다.**

### ★★값을 꺼내는 진짜 주소

```
GET  /api/v1/secrets/{시크릿ID}/versions/{버전}/value
```

⚠️**내가 0805 에 헤맨 것** — `POST .../secret-string` 을 시도해 **405** 를 받고
"막혔다"고 판단했다. **경로가 틀렸을 뿐**이고, 맞는 주소는 **처음부터 코드 안에 있었다.**

응답 모양 (0731 확인):

```json
{"version": {"secret": {"JWT_SECRET_KEY": "…"}}}
```

★**키가 이미 환경변수 이름이라 매핑표가 없다.** 시크릿을 만들 때 그 규칙으로 넣기로 했다.

### 안전장치 (코드에 들어 있음)

```
_FORBIDDEN      PATH · LD_PRELOAD · PYTHONPATH · SECRETS_* 등은 시크릿에 있어도 주입 안 함
                → 금고를 쓸 수 있는 사람이 앱 실행 방식까지 바꾸지 못하게
_NAME_RE        대문자로 시작하는 환경변수 형식만 허용
오류 본문 미출력  HTTPError 본문을 예외에 안 담는다 — 값이 로그로 샐 수 있어서
실패 시 예외      조용히 .env 로 안 떨어진다. 설정 누락이 엉뚱한 증상으로 둔갑하는 걸 막음
LoadResult      ★이름과 개수만 담는다. 값은 안 담는다 → 안전하게 검증 가능
```

---

## 4. 그래서 K8s Secret 에 실제로 들어가는 것

★**로더가 실제로 주입한 것 6개** (0805 구동 확인 · 값 미출력)

```
JWT_SECRET_KEY   SMTP_APP_PASSWORD   STT_WORKER_TOKEN
METRICS_INGEST_TOKEN   PORTONE_API_SECRET   PORTONE_CHANNEL_KEY
```

```
✅ SECRETS_ACCESS_KEY      catchap-backend 액세스 키 ID (32자)
✅ SECRETS_SECRET_KEY      catchap-backend 보안 액세스 키 (70자)
❓ DATABASE_URL            ← 금고에 없음. 자격증명 2-3 (catchap_backend DB 계정)
❓ MEDIA_S3_ACCESS_KEY     ← 금고에 없음. 자격증명 1-2 (S3 키)
❓ MEDIA_S3_SECRET_KEY
❓ MAIN_CAPTCHA_SITE_SECRET  ← 캡차와 나눠 갖는 값. 금고에 없음
❓ PORTONE_STORE_ID          ← 금고의 catchap-portone-keys 에 안 들어 있다
❓ PORTONE_WEBHOOK_SECRET    ← 위와 같음

⛔ 위 6개   ← 넣지 않는다. 앱이 기동할 때 금고에서 읽는다
```

★**ConfigMap 으로 갈 것** (비밀 아님 — 저장소 문서에 이미 공개돼 있다)

```
SMTP_USER = 09.daviya.09@daviya.kr     ← 로그인 계정. ★이게 비면 부팅 거부
MAIL_FROM = catchap5@catchap5.com      ← 화면에 보이는 발신자
```

★★**0805 결정 — (ㄱ) 로 확정.** 사장님이 고르셨다. 금고에 시크릿을 하나 더 만든다.
**누르는 절차는 [92-킵-대기/03-금고-시크릿-추가.md](92-킵-대기/03-금고-시크릿-추가.md) 에 있다.**

```
이번에 넣는 것 3     DATABASE_URL · MEDIA_S3_ACCESS_KEY · MEDIA_S3_SECRET_KEY
                   → 자격증명 195·196행(DB) · 39·41행(S3). ★자리 검증 완료
못 넣는 것 3        MAIN_CAPTCHA_SITE_SECRET(회전 예정) · PORTONE_STORE_ID
                   PORTONE_WEBHOOK_SECRET  ← 값을 아직 모른다. 백엔드 기동은 막지 않는다
만든 뒤             k8s/backend/10-configmap.yaml 의 SECRETS_NAMES 에 이름 한 개 추가
```

**아래는 결정 전에 적어 둔 두 갈래다. 근거를 남긴다.**

```
(ㄱ) 금고에 시크릿을 하나 더 만든다      catchap-db-and-storage 같은 이름
     → K8s Secret 은 부트스트랩 키 2줄만. ★가장 깨끗
     → 사장님이 콘솔에서 만드셔야 함
(ㄴ) 이 3건만 K8s Secret 에 둔다
     → 지금 바로 됨. 대신 상태가 두 곳
```

★**아직 안 정했다. 사장님께 여쭐 것.**

---

## 5. ✅확인한 위험 — 0805 에 전부 실측으로 지웠다

```
✅ 워커 노드에서 금고에 닿나?      임시 파드로 확인 (측정 노드 host-10-0-6-202)
     IAM 200 · Secrets Manager 401 · Object Storage 404   ← 401/404 = 닿았다는 뜻
✅ catchap-backend 가 5건 전부의 접근 명단에 있나?   5건 다 읽혔다
✅ 변수 이름이 백엔드가 기대하는 것과 맞나?          6개 다 config.py 의 설정 이름과 일치
```

### ★★5-A. kubectl 인증서를 고쳤다 (0805) — 내가 만든 문제였다

```
전   /home/ubuntu/.catchap/ca.crt = ★apiserver 서버 인증서 그 자체 (CN=kube-apiserver)
     Go 가 받아 주긴 해서 "대체로 동작"했다.  get ns 10번 중 1번 · logs 5번 중 1번 실패
후   진짜 CA (CN=kubernetes, kube-public/kube-root-ca.crt)
     ★get ns 20번 · logs 20번 전부 성공.  원본은 ca.crt.leaf-백업-0805
```

★**더 큰 이유는 만료다** — 고정했던 서버 인증서는 **2027-07-28** 만료라, 서버가 인증서를
갱신하는 순간 **모든 접근이 끊긴다.** 진짜 CA 는 **2036-07-25**.

★**교훈 = 「대체로 동작함」은 맞다는 증거가 아니다.** 간헐적 실패를 네트워크 탓으로
넘겼으면 1년 뒤 갑자기 전부 끊겼을 것이다.

---

## 6. 오늘(0805) 여기까지 한 것

```
✅ 이미지 안에서 secrets_loader.py 존재 확인 · 전문 읽음 · 환경변수 계약 확보
✅ 값 조회 주소가 GET …/value 임을 확인 (405 는 경로 오류였음)
✅ 「값을 K8s 로 복사」 노선을 폐기 — 설계 2-E 대로 되돌림
✅ ★로더를 실제로 구동 — 시크릿 5건 읽고 변수 6개 주입 성공 (값 미출력)
✅ ENV=prod 로 Settings 생성 시도 → 모자란 것은 SMTP_USER 하나
✅ ★백엔드는 Valkey 를 안 쓴다 — 코드에 redis/valkey 참조 0건
✅ ★★behavior-ai 이미지를 옛 서버에서 발견 (아래 6-A)
```

### 6-A. ★★behavior-ai — 「소스가 없다」는 전제가 틀렸다

옛 백엔드 서버 `210.109.52.124` 에서 **컨테이너가 7일째 돌고 있다.**

```
catchap-backend-api-1   catchap-backend:latest       Up 16 hours (healthy)
★catchap-behavior-ai    catchap-behavior-ai:latest   Up 7 days (healthy)
catchap-backend-caddy-1 caddy:2-alpine               Up 2 weeks
```

★**소스를 sw님께 물을 필요가 없다.** `docker save` → 전송 → CR 로 push 하면 된다.

⚠️★**0805 저녁 정정 — 이 이미지가 「라이브」가 아니었다.** 옛 백엔드 서버의 도커 이미지는
아무도 안 부르는 것이었고, 캡차가 실제로 부르는 것은 **옛 GPU `/home/sw/catchap-behavior`**
였다. 그 소스로 다시 구워 **`feea9a597269`** 로 배포했다(위 0-0 참조).

### 6-B. ★낡은 문서가 나를 헷갈리게 했다

`k8s/backend/20-secret.예시.yaml` 33행에 이렇게 적혀 있었다.

> *"★나중에 — Secrets Manager 에서 직접 읽어 오게 바꾼다. **지금은 클러스터 권한이 없어**
> 그 연동을 못 얹으므로 kubectl 시크릿으로 시작한다."*

★**권한은 0804 에 풀렸고 로더는 이미 이미지 안에 있다.** 전제가 사라졌는데 파일이 안 고쳐져서,
내가 그걸 보고 (나) 로 갔다. **→ 이 파일을 다시 쓸 것(할 일 4번).**

### 6-C. ⚠️오늘 낸 유출 2건 — 숨기지 않고 적는다

```
① pydantic 예외를 그대로 출력   JWT_SECRET_KEY 앞 5자·뒤 7자 노출
   원인  ValidationError 의 str() 에 input_value 가 들어간다
   대책  ★str(e) 를 만들지 않는다. e.errors()[i]["msg"] 만 쓴다
② Grep 내용 검색                CAPTCHA_SITE_SECRET 전체 노출
   원인  _ms-captcha-extract/drag-captcha/.env 가 ★평문으로 작업 폴더에 있다
   대책  컷오버 때 회전(캡차 README 102행에 이미 권고). ★이제 권고가 아니라 필수
```

---

## 7. 다음에 할 것 — 순서대로

```
✅1 catchap-backend 키로 로더를 실제로 돌림 — 5건 읽고 6개 주입 (값 미출력)
✅4 backend/20-secret.예시.yaml 을 부트스트랩 키만 담게 다시 씀
✅  backend/10-configmap.yaml 에 SECRETS_* · SMTP_USER · MAIL_FROM 추가
✅  behavior-ai 이미지 확보 → CR push → 매니페스트 4벌 → ★서버 스키마 검증 통과

2  워커 노드에서 IAM·Secrets Manager 엔드포인트 도달 확인   ← ★아직 안 함. 위험 5번
3  ❓ 6건을 (ㄱ)/(ㄴ) 중 어느 쪽으로 할지 사장님께 여쭘      ← ★막고 있는 것
5  backend 배포 → api.catchap5.com 이 503 → 200 인지 확인
6  behavior-ai 배포 → captcha 배포 (★순서 지킬 것. 캡차가 AI 를 부른다)
7  컷오버 (가비아 3~4줄) — [92-킵-대기/02-콘솔작업.md](92-킵-대기/02-콘솔작업.md) ⑤
```

### ★behavior-ai — 0805 에 풀린 것 (자세한 건 [k8s/behavior-ai/README.md](../k8s/behavior-ai/README.md))

```
포트 8010 · uvicorn app.main:app · uid 10001 · 볼륨 없음 · 디스크 쓰기 없음
메모리 153.4MiB · CPU 0.26%  (옛 프로덕션에서 docker stats 로 실측)
★PRODUCTION_MODEL_DIR 기본값 models/production 은 이미지에 없다.
  실제 = models/candidate/revalidation_two_view_participant_safe_20260722
  → 기본값으로 두면 모델이 말없이 안 뜬다(앱은 그래도 200 을 준다)
★/health 는 DB 가 죽어도 200 을 준다. 본문에만 degraded.
  → liveness 는 알맞고, readiness 는 DB 끊김을 못 잡는다. shadow 라 지금은 괜찮다
★열쇠 3개가 각각 다른 길: /collect · /challenge(캡차) · /admin
```

⬜ **API 열쇠 3개의 값을 못 읽었다** — 옛 서버 환경변수 읽기가 안전장치에 막혔다.
캡차 쪽과 짝이 맞아야 하므로 **컷오버 때 양쪽을 같이 새로 만드는 것**을 권한다.

---

## 8. 잊으면 안 되는 제약

```
★ 콘솔 버튼은 내가 안 누른다. 주소 이동 · 캡처 · DOM 읽기 · 정리만.
★ 비밀값은 화면에 안 찍는다. 길이로만 검증한다.
★ ENV=prod 는 스위치가 아니라 관문 — JWT 32자 · CORS 에 * 금지 · SMTP 필수를
  부팅 때 검사하고 하나라도 걸리면 ★부팅을 거부한다. 13건이 다 있어야 뜬다.
★ 캡처 이야기는 내부 문서에만. 강사님 보고에는 안 쓴다.
```

---

## 2026-08-10 새벽 — 캡차·행동AI 비밀값을 금고로

```
✅ 금고 시크릿 11건 → ★19건 (신설 8 · 기존 3건에 키 추가) · 전부 kms11
✅ 캡차·행동AI 에 로더 이식 — 백엔드 것을 ★그대로 옮김 (새 방식 안 만듦)
✅ 클러스터 비밀값 15개 → ★7개 (열쇠 3쌍 + registry 1)
✅ ★라이브 확인 — K8s 값 지운 뒤 도메인으로 캡차 발급 201 · 조각 이미지 200 image/png
✅ 적대적 검토에서 나온 「부분 실패가 조용했다」를 고침 (SECRETS_REQUIRED_VARS)
   + 캡차 db_user 기본값이 catchap_dba(DDL 계정)였던 것을 빈 값으로

★왜 그렇게 정했나
  묶는 기준 = 「앱」이 아니라 ★「같이 회전하는가」
    캡차 BEHAVIOR_AI_BACKEND_KEY == 행동AI CAPTCHA_BACKEND_API_KEY (sha 6df97ef086)
    백엔드 MAIN_CAPTCHA_SITE_SECRET == 캡차 CAPTCHA_SITE_SECRET     (sha 01069988aa)
    → 앱별로 묶으면 같은 값이 두 곳에 생겨 ★한쪽만 회전하면 조용히 깨짐
  이사와 회전을 ★분리 — 같이 하면 문제가 났을 때 원인을 못 가림
    ⚠️그래서 금고의 ASSET_S3_* 는 ★아직 옛 계정 키. 그 계정 지우면 캡차 이미지가 죽음

✅결정 — 금고 시크릿별 접근 제어는 ★안 합니다 (0810 사장님)
  kms11 명단이 1개 → 3개가 되면서 ★열쇠 하나가 시크릿 19건 전부를 엽니다(실측)
  ★그래도 안 하는 근거 = 19건×3계정 중 한 건만 틀리면 전면 장애 ·
    앞 관문 두 겹(RBAC·KMS)이 살아 있음 · 열쇠는 즉시 폐기 가능
  ★대신 문서로 남김 — 91-팀공유/16(누가 무엇을 쓰나) · 92-킵-대기/13(알고도 안 한 것)
```
