이름: 김태형

담당 역할: 인프라

프로젝트 전체 진행률: 90%
개인 목표 진행률: 95%
전날 계획 대비 달성률: 100%

어제 한 일 : 로드밸런서 이중화 완료(두 번째 가용 영역에 로드밸런서·대상 그룹·리스너 신설 후 고가용성 그룹으로 묶어 단일 장애점 제거), 도메인 전환 절차 확정, 네트워크 자원 이름 표준화(서브넷 2개 삭제 후 4개 이름 통일), 쿠버네티스 배포 권한 확보(권한 문제가 아니라 잘못된 계정으로 확인하고 있었음을 발견), 배포 전용 계정으로 권한 이관 및 임시 자격증명 폐기, 인그레스 컨트롤러 설치, 워커 노드 보안 그룹에 상태 검사 IP 허용, 화면(프론트엔드) 배포(서로 다른 가용 영역에 2벌), 루트 도메인을 www로 넘기는 규칙 추가, 로드밸런서 상태 정상 확인

오늘 할 일 : (저녁 추가) 팀이 오늘 완료한 개발분을 새 환경에 반영 - 기존 서버에서 도는 이미지를 그대로 가져와 서버·화면 교체 완료, 행동 판별 서비스는 처음 가져온 것이 실제 운영본이 아님을 파일 대조로 발견해 다시 제작(상태 점검 응답만으로는 구분되지 않았고 파일 크기를 맞춰 봐야 드러났습니다), 캡차는 코드는 같고 화면 위젯만 오늘 새로 만들어진 것을 확인해 그 부분만 갱신, 네 서비스 모두 기존 서버 기준으로 일치시켰습니다. 또한 GitHub 저장소·브랜치 재설계안과 자동 배포 설계안을 문서로 정리했습니다(실행은 대표님과 함께 진행). / 비밀값 관리 방식을 설계대로 원복(앱이 기동 시 비밀 보관소에서 직접 읽는 방식으로 전환, 쿠버네티스에는 보관소 열쇠 두 줄만 남김 - 완료), 그 동작을 실제로 구동해 검증(완료), 행동 판별 서비스 이미지 확보(기존 서버에서 동작 중인 것을 그대로 이전 - 완료), 쿠버네티스 접속 인증서 설정 오류 수정(간헐적 접속 실패 원인 - 완료), 클러스터에서 비밀 보관소 도달 확인(완료), 비밀 보관소에 항목 3건 추가(완료 - 접속 정보와 저장소 키, 캡차 공유 비밀), 넣기 전에 모든 값이 실제로 통하는지 확인(데이터베이스 접속·저장소 조회·메일 로그인 - 완료), 서버(백엔드) 배포(완료 - 2벌, 서로 다른 가용 영역), 행동 판별 서비스 배포(완료 - 모델 적재 확인), 캡차 배포(완료 - 행동 판별 서비스 연동 동작 확인), 밖에서 들어오는 경로 6개 전수 확인(완료), 도메인 전환 준비 완료

이슈 및 막힌 부분 (기술적 어려움, 협업 문제 등): 먼저, 비밀 보관소 점검 중에 배포 전용 계정으로도 운영 비밀 항목 5건이 전부 조회되는 것을 확인했습니다. 그 계정의 키는 빌드 서버에 상시 보관되어 있어, 빌드 서버에 접근할 수 있으면 로그인 서명 키와 결제 키까지 닿습니다. 새로 만드는 항목부터 접근 제어를 걸고, 기존 5건은 도메인 전환이 끝난 뒤 순차 적용합니다(배포 중에 적용하면 앱이 값을 못 읽는 사고가 날 수 있습니다). 둘째, 작업 중 비밀값 일부가 화면에 출력되는 일이 두 번 있었습니다. 제가 직접 출력한 것이 아니라 오류 메시지와 파일 검색 결과에 값이 섞여 나온 것으로, 해당 값은 도메인 전환 시점에 교체하기로 하고 재발을 막는 절차를 문서화했습니다. 셋째, 기존 서버에서 동작 중인 프로세스의 설정값을 읽는 작업이 보안 정책으로 차단되어, 결제 연동 키 2건과 캡차 공유 키 1건의 값을 아직 확보하지 못했습니다. 이 3건이 없어도 서버는 기동되며 결제와 캡차 검증 기능만 나중에 켜집니다. 넷째, 어제까지 "담당자에게 소스 위치를 확인해야 한다"고 보고했던 행동 판별 서비스는 기존 서버에서 완성된 형태로 동작 중인 것을 발견해 소스 없이 그대로 이전했습니다. 협업 대기 항목이 하나 없어졌습니다
