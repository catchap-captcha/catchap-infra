# CatChap 클라우드 이전 진행 보고

1조 김태형 · **2026-07-30 23:55 기준** · 프로젝트 `kc-sfacspace05`

## 한 줄 요약

**서버와 인프라 준비는 거의 끝났고, 이전의 실체인 이미지 빌드·배포는 아직 시작하지 않았습니다(0%).**
전체 45개 항목 중 **20개 완료 = 44%** 입니다 — **남은 25개**. (18:00 보고 39%에서 +5%p)

★2026-07-30 23:55 갱신 — **서버 5대를 실제로 손보고 STT 워커까지 띄웠습니다.**
GPU에 Docker·NVIDIA 툴킷을 넣어 컨테이너에서 GPU가 보이는 것을 확인했고, 5대 시간대를
KST로 통일했으며, **5대 전부 업데이트·재부팅**을 마쳤고, **GPU에서 실제로 음성이 자막으로
나오는 것까지 확인**했습니다(7.9초 → 1.33초).
**서버 5대가 전부 "남은 업그레이드 0개 · 재부팅 불필요"** 상태입니다.

★진행률은 **아래 표의 항목 수를 세어 계산**했습니다. 느낌이 아니라 셀 수 있는 항목 기준입니다.

---

## 단계별 진행률

| 단계 | 완료 / 전체 | 진행률 | 상태 |
|---|---|---|---|
| **A. 인프라 준비 (콘솔)** | 13 / 15 | **87%** | 남은 2건은 보류·후속 (IP 좁히기 불가 · GPU SG는 후속) |
| **A'. 서버 준비 (SSH)** | 5 / 6 | **83%** | ★오늘 신설 — GPU 컨테이너·시간대·업데이트·**STT 워커 가동** |
| **B. 강사님 대기** | 0 / 3 | **0%** | 대체 수단 없음 — 여기서 막혀 있습니다 |
| **C. 이미지 빌드·푸시** | 0 / 6 | **0%** | 방식은 결정, 실행 미착수 |
| **D. K8s 매니페스트·배포** | 0 / 4 | **0%** | B가 풀려야 배포 가능 (작성은 지금 가능) |
| **E. 무상태화 (앱 코드)** | 0 / 3 | **0%** | 인프라는 3/3 준비됨, 코드가 아직 안 씀 |
| **F. 운영 준비** | 2 / 8 | **25%** | 자가검토 1건 + **토큰 회전 1건** 추가 발견 |
| **합계** | **20 / 45** | **44%** | 18:00 보고 15/38 = 39% 에서 **+5개 완료** |

★**항목이 38 → 45개로 늘었습니다.** 서버 작업(A' 6건)을 원래 F단계에 뭉뚱그려 넣어 뒀는데
실제로 해 보니 별도 단계로 셀 만했고, 작업하면서 **F에 2건이 새로 나왔습니다**(F-7·F-8).
**분모를 같이 늘렸습니다** — 분모를 그대로 두고 완료만 더하면 진행률이 부풀려집니다.
그래서 **5건을 끝냈는데도 39% → 44%로 5%p만 올랐습니다.**
★일이 진행되면 **몰랐던 항목이 드러나 분모가 커집니다.** 그게 정상이고, 그걸 숨기면
진행률이 실제보다 좋아 보입니다.

---

## A'. 서버 준비 (SSH) — 83% (5/6) ★오늘 밤에 한 일

**콘솔이 아니라 서버에 직접 들어가서 한 작업입니다.** 화면이 없어 명령 출력 원문을 남겼습니다.

| | 한 일 | 결과 |
|---|---|---|
| 1 | **GPU에 Docker + NVIDIA Container Toolkit** | Docker 29.6.2 · 툴킷 1.19.1 · **컨테이너 안에서 Tesla T4 인식 확인** |
| 2 | **5대 시간대 `Asia/Seoul`** | 전부 `Etc/UTC`였음 → KST |
| 3 | **VM1~VM4 전체 업데이트 + 재부팅** | 150 → **0개** · 커널 `6.8.0-39` → **`6.8.0-136`** |
| 3' | **GPU 업그레이드 + 재부팅** | 148 → **0개** · 재부팅 후 드라이버·컨테이너·**전사까지 자동 복구** |
| 4 | **GPU STT 워커 컨테이너 기동 (`:8100`)** | **가동 중** — 7.9초 음성을 **1.33초**에 전사 · GPU 3.9GB 점유 |
| 5 | 실제 강의 음성으로 한국어 정확도 확인 | **미완** — 합성 음성으로는 판단할 수 없습니다(아래) |

**4번 검증 — "떴다"가 아니라 "GPU로 전사가 된다"까지 봤습니다**

```
/health          {"ok":true,"model":"large-v3","device":"cuda","loaded":true}
GPU 점유          nvidia-smi 컴퓨트 프로세스 3,846 MiB
실제 전사(영어)    7.9초 음성 → 1.33초 (약 6배속) · 문장 거의 그대로
인증             토큰 없이 401 · 토큰 맞게 통과
무음             422 "no speech segments"  ← 가짜 성공을 안 만든다
도달성           VM1 · VM2 · K8s 워커 2대(다른 AZ 포함) 전부 /health 200
복구             크래시 → 자동 재시작 · docker 데몬 재시작 → 자동 복귀
```

★`docker ps`가 `Up`이어도 모델이 CPU로 떨어져 있을 수 있어서, **GPU 점유와 실제 전사**를 봤습니다.

**★5번 — 한국어는 "된다"고 말하지 않겠습니다**

합성 음성(`espeak-ng`)으로 시험했는데 `안녕하세요`는 정확했지만 나머지 문장은 어긋났습니다.
**`espeak-ng`의 한국어는 사람 발음과 많이 달라서 이 결과로는 판단할 수 없습니다.**
모델이 나쁘다는 근거도, 좋다는 근거도 아직 없습니다 → **실제 강의 음성으로 다시 봐야 합니다.**

**1번을 "설치 완료"가 아니라 이렇게 검증했습니다**

```
docker run --rm --gpus all ubuntu:24.04 nvidia-smi   →  Tesla T4, 580.173.02, 15360 MiB
순정 이미지에는 없는 libcuda.so / libcuda.so.1 이 컨테이너 안에 주입된 것까지 확인
```

★`nvidia-smi` 한 줄만 보면 "드라이버가 보인다"까지만 확인한 셈이라 부족합니다.
**툴킷이 실제로 일했는지**를 봐야 STT 워커가 GPU를 쓸 수 있다고 말할 수 있습니다.

**3번을 지금 한 이유** — **아직 아무 서비스도 안 올라간 지금이 재부팅이 가장 싼 시점**입니다.
보안 업데이트는 0건이라 급한 건 아니었지만, 이전이 끝난 뒤에 재부팅하면 서비스가 멈춥니다.
★VM1(점프서버)은 **마지막에**, 재부팅 전 `sshd -t`로 설정 문법을 확인하고 했습니다.
못 돌아오면 나머지 4대가 전부 고립되기 때문입니다.

**★GPU는 한 번 미뤘다가 23:5x에 마저 했습니다 — 미룬 근거는 틀렸습니다**

미룰 때 든 이유는 이것이었습니다.

> GPU에 `nvidia-dkms-580-server`가 있어 **커널이 올라가면 드라이버를 다시 빌드**합니다.
> 빌드가 실패하면 방금 확인한 T4 인식이 깨집니다.

**확인해 보니 이번 경우엔 그 위험이 없었습니다.**

```
148개에 커널 이미지·nvidia·dkms 패키지  →  0개 · 제거되는 패키지도 0개
출처  noble 6 · noble-updates 149 · noble-security 0
GPU 실행 커널 6.8.0-136 = 설치된 최신 커널     (업그레이드한 VM들과 같은 커널)
```

조사 때 `(보안 0 · 커널 1)`로 셌던 "커널 1"은 **커널 이미지가 아니라 `linux-base`** 였습니다.
**패키지 개수만 보고 이름을 확인하지 않았습니다.** → **"커널 관련 1건"은 "커널이 올라간다"가
아닙니다.** 미룬 것 자체는 손해가 없었지만(보안 0건) **근거는 정정합니다.**

**GPU 업그레이드 결과** — 148 → **0개** · `EXIT=0` · 제거 0건 · 미합병 설정 0개.
재부팅 전 `sshd -t`로 검사하고(ssh가 갱신 대상이었습니다) 재부팅했습니다.

```
재부팅 후   드라이버 580.173.02 · DKMS 그대로 · docker active
           ★STT 컨테이너 40초 만에 자동 복구 · 컨테이너 안 GPU 인식 · 실제 전사 성공
```

★★**이 재부팅으로 미검증 경로가 하나 닫혔습니다** — 그전까지는 크래시와 docker 데몬
재시작만 시험했고 **"호스트를 재부팅해도 워커가 살아나는가"는 확인 못 했습니다.**

★**업그레이드 중 `NEEDRESTART_MODE=l`을 썼습니다** — 기본값이면 도중에 서비스를 자동
재시작해 **docker와 STT 컨테이너가 예측 못 할 시점에 끊깁니다.** 목록만 만들게 하고
재부팅으로 한 번에 정리하는 편이 순서가 분명합니다.

**★남은 위험 하나 — 자동 업그레이드**

`unattended-upgrades`가 **5대 전부 켜져 있습니다.** 지금은 보안 대기가 0건이라 아무 일도
안 하지만, **앞으로 커널 보안 업데이트가 나오면 우리 모르게 설치되고 드라이버가 재빌드됩니다.**
`apt-mark hold`로 막으면 커널 보안 패치를 영구히 못 받으므로 권하지 않습니다.
→ **커널이 올라간 뒤 `nvidia-smi`를 확인하는 절차**를 운영 항목(F-7)으로 둡니다.

**최종 상태 (23:52 KST) — 5대 전부 동일**

```
VM1 10.0.1.241   6.8.0-136  KST  남은 0  재부팅 no
VM2 10.0.2.165   6.8.0-136  KST  남은 0  재부팅 no
VM3 10.0.2.212   6.8.0-136  KST  남은 0  재부팅 no
VM4 10.0.2.21    6.8.0-136  KST  남은 0  재부팅 no
GPU 10.0.5.57    6.8.0-136  KST  남은 0  재부팅 no   · STT 워커 가동 중 · GPU 3,892 MiB
```

---

## A. 인프라 준비 — 87% (13/15)

**오늘 실행한 변경 8건** (전부 변경 전후 캡처로 검증)

| | 한 일 | 결과 |
|---|---|---|
| 1 | 워커 보안 그룹에 TCP 80 인바운드 추가 | 인바운드 10 → 11개 · 출발지 `10.0.0.0/16` |
| 2 | 로드밸런서 대상 포트 정정 | `443` → `80` · Active · 100% |
| 3 | 운영 서비스 계정 `catchap-ops` 신설 + IAM 키 | 역할 6개(실효 11) |
| 4 | Container Registry 저장소 이름 오타 정정 | `chatchap-frtend-repo` → `catchap-front-repo` |
| 5 | 낡은 서비스 계정 2개 **삭제** | 미사용 전환 → **Cloud Trail 이벤트 각 0건 검증** → 삭제. 계정 6 → 4건 · 쓰기 주체 3 → 2개 |
| 6 | 퍼블릭 SG에서 `6379` 규칙 제거 | 인바운드 4 → 3개 |
| 7 | MySQL Standby `3307` 개방 | 인바운드 4 → 5개 · Read Replica 사용 가능 |
| 8 | 시험용 시크릿 `catchap-team1-probe` 삭제 | 시크릿 7 → 6건 (우리 프로덕션 4 + 강사님 2) |

**조회로 결론 낸 것 5건** (변경 0건)

```
catchap-ops 키 실동작 API 전수 검증     → 강사님 요청 2건 소멸
CR push 권한 진단                    → 프로젝트 멤버 이상 필요 확정
보안 그룹 전수 점검                   → 우리 SG 4개 확인
Object Storage 4항목 확인             → 버킷 권한·비공개·용량·Lifecycle
접근 주체 확인                        → kdt5 그룹 15명이 조회 전용으로 들어와 있음
```

**남은 2건** (13번은 이미 끝났는데 이 표에 남아 있던 것 — 위 변경 8건의 마지막과 같은 항목입니다)

| | 항목 | 상태 |
|---|---|---|
| 13 | ~~시크릿 `catchap-team1-probe` 삭제~~ | ✅ **완료** — 위 표 8번. 시크릿 7 → 6건 |
| 14 | 새 VPC GPU의 보안 그룹 정정 | **★전제 충족 · 사장님 클릭만 남음** — 8100 호출 4대 전부 도달 확인. 파드에서 나가도 노드 IP로 SNAT되므로 `10.0.0.0/16:8100`으로 커버됨 |
| 15 | 퍼블릭 SG `22`를 특정 IP로 좁히기 | **보류** — 팀원 3명 접속 IP를 먼저 모아야 함. 지금 좁히면 우리가 잠김 |

---

## B. 강사님 대기 — 0% (0/3) ★여기서 막혀 있습니다

| | 항목 | 왜 대체가 안 되나 |
|---|---|---|
| ① | **Kubernetes `cluster-admin` 바인딩** | 클러스터 내부 RBAC. **전용 IAM 역할이 아예 없어** 프로젝트 역할을 아무리 받아도 대체 불가. 실제로 `프로젝트 관리자`를 받았지만 클러스터 API는 여전히 `403` |
| ② | 시크릿 `catchap-backend-prod` 삭제 | 접근 제어가 걸려 **체크박스 자체가 비활성**. ID도 마스킹돼 API로도 불가 |
| ③ | Deployflow 회신 (예/아니오) | 우리 클러스터는 프라이빗 엔드포인트라 Deployflow 대상이 될 수 없음 |
| ④ | ~~`catchap-backend2`가 강사님 것인지 확인~~ → **사후 보고로 성격이 바뀜** | **Cloud Trail 이벤트 0건**(생애 전체)을 확인하고 **삭제했습니다.** 강사님이 만드신 것이었다면 알려 주십사 하는 통보이지, 막혀 있는 항목이 아닙니다 |

★**요청이 6건 이상 → 실제 2건 + 회신 2건으로 줄었습니다.** 나머지는 직접 확인해서 없앴습니다.

```
불필요  kms11 허용 명단 추가        실측으로 시크릿 4건 값까지 읽힘
불필요  버킷 권한 부여              버킷에 역할 그룹 매핑이 이미 6건 있음
아님    CR push 권한               저장소별 부여는 "프로젝트 밖" 사용자용. 우리가 처리
아님    MySQL 비밀번호             콘솔에 기능이 없는 건 맞지만 SQL로 우리가 함
직접함  워커 SG 80 · LB 대상 · 계정 정리 · 저장소 이름
```

---

## C. 이미지 빌드·푸시 — 0% (0/6)

| | 항목 | 상태 |
|---|---|---|
| 1 | 빌드 호스트 결정 | **미정** ← 여기가 먼저입니다 |
| 2 | Docker 설치 | 미착수 — 이 PC와 **VM1~VM4에 없습니다**. ★GPU에는 0730에 설치했지만 STT 전용으로 둡니다 |
| 3 | Dockerfile 정본 정리 | 미착수 — ★**저장소 루트의 2개가 0바이트**이고 실제 내용은 워크트리에만 있음 |
| 4 | 이미지 빌드 | 미착수 |
| 5 | push 자격증명 발급 | **방식은 결정** — `프로젝트 멤버`를 가진 서비스 계정. 빌드 시점에 발급 |
| 6 | push | 미착수 — 저장소 3개 모두 이미지 0개 |

★**결정된 것** — push는 `프로젝트 멤버` 이상이 필요하고(실측 `actions:["pull"]`), 저장소별
`리포지토리 멤버`로는 우회할 수 없습니다(콘솔이 서비스 계정을 후보에서 제외). pull(런타임)은
`catchap-ops`로 이미 됩니다.

---

## D. K8s 매니페스트·배포 — 0% (0/4)

| | 항목 | 상태 |
|---|---|---|
| 1 | 매니페스트 작성 (백엔드·프론트·AI·인그레스·ConfigMap·Secret·Service) | **미착수 — 권한 없이 지금 할 수 있는 가장 큰 작업** |
| 2 | 인그레스 컨트롤러 배포 | B①(`cluster-admin`) 필요 |
| 3 | 매니페스트 적용 | B① 필요 |
| 4 | `catchap5.com` 도메인 접속 확인 | 위 이후 |

★배포 준비는 되어 있습니다 — 로드밸런서에 **우리 SAN 인증서가 이미 붙어 있고**, 대상 포트도
오늘 `80`으로 맞췄으며, 워커 SG에 `80`을 열었습니다. **인그레스만 올라오면 연결됩니다.**

---

## E. 무상태화 (앱 코드) — 0% (0/3)

**인프라는 3/3 준비됐고 앱 코드가 아직 그걸 쓰지 않습니다.**

| | 항목 | 인프라 | 코드 |
|---|---|---|---|
| 1 | 세션 → Valkey(MemStore) | ✅ Running · 접속 확인 · 비밀번호 불필요 | ❌ 아직 메모리 세션 |
| 2 | 미디어 → Object Storage | ✅ 버킷·권한·비공개 확인 · 약 2.4GB 이전 대상 | ❌ 아직 로컬 파일 |
| 3 | `.env` → Secrets Manager | ✅ 시크릿 4건 이관 완료 | ❌ 앱은 여전히 `.env` 읽음 |

★**이것이 K8s로 가는 실제 이유**입니다. 지금 구조로는 서버를 늘리면 로그인이 풀리고
미디어가 서버마다 흩어집니다.

---

## F. 운영 준비 — 25% (2/8)

| | 항목 | 상태 |
|---|---|---|
| 1 | GPU 드라이버 설치·검증 | **완료** (0729) — 580.173.02 · FP32 3.81 / FP16 22.8 TFLOPS. ★컨테이너화는 위 A'-1 |
| 2 | MySQL 백업 확인 | **완료** — 자동 백업 4건 · 매일 01:00 · 14일 보존 |
| 3 | VM 모니터링 에이전트 설치 | 미착수 — ★**안 깔면 Monitoring 화면에 안 보입니다** |
| 4 | MySQL 관리자 비밀번호 회전 | 미착수 — 평문 공유 상태. SQL로 우리가 함(앱·팀원·Secrets Manager 동시) |
| 5 | `catchap-ops` 키 회전 | 미착수 — 발급 화면 캡처가 팀에 공유되므로 이전 후 1회 |
| 6 | SSL 인증서 갱신 | 미착수 — **2026-10-20 만료 · acme.sh 갱신이 수동** |
| 7 | **커널 자동 업그레이드 후 GPU 확인 절차** | **미착수 · ★오늘 자가검토에서 새로 찾은 항목** — `unattended-upgrades`가 5대 전부 켜져 있어 커널 보안 업데이트가 자동 설치되고, 그때 GPU 드라이버가 재빌드됩니다. STT 워커 헬스체크로 잡히게 합니다 |
| 8 | **STT 토큰 회전 (3곳)** | **미착수 · ★제 실수로 옛 토큰 값이 노출됐습니다** — 새 워커는 새 토큰을 쓰지만, 옛 값이 **옛 GPU 박스 워커 · 프로덕션 백엔드 `.env` · Secrets Manager** 세 곳에 살아 있습니다. **컷오버 때 세 곳을 한 번에** 바꿔야 합니다(따로 하면 전사가 멈춥니다). 상세는 [00-작업기록.md](../00-작업기록.md) 18-B |

---

## 다음에 할 일 (권한 없이 지금 가능한 순서)

```
1  GPU STT 워커 컨테이너 기동     A'-4. Docker·툴킷·GPU 인식까지 끝나 바로 이어짐
2  Dockerfile 정본 정리          루트 2개가 0바이트. 빌드 전 필수
3  빌드 호스트 결정 + Docker 설치   VM2·3·4 중 하나 (셋 다 비어 있음)
4  K8s 매니페스트 작성           D①. 가장 큰 작업이고 권한이 필요 없음
5  미디어 2.4GB 이전             전 구간 실측이 끝난 유일한 작업
6  앱이 Secrets Manager를 읽게    E③
```

★**강사님 답을 기다리는 동안 할 수 있는 일이 대부분입니다.** 막힌 것은 **배포 실행 한 지점**뿐입니다.

---

## 근거

| | |
|---|---|
| 콘솔 화면 캡처 | **363장 · 중복 0** (파일 해시 전수 대조 · 본 폴더 299 + 1차본 64) |
| 실행 내역 | [00-작업기록.md](../00-작업기록.md) — 항목마다 **왜·경로·변경 전후·검증·영향·되돌리기** |
| 현황·설정값 | [00-정리노트.md](../00-정리노트.md) |
| 강사님 요청 정본 | [00-강사님-요청서.md](../90-강사님보고/99-옛-요청서-대체됨/01-요청서-0730.md) · 대면용 요약 [00-강사님-요약-대면용.md](../90-강사님보고/99-옛-요청서-대체됨/02-요약-대면용-0731.md) |
| 목표 구조 | [00-아키텍처-설계.md](../00-아키텍처-설계.md) — 컷오버는 **A 레코드 한 줄** |
| 작업 과정 캡처 | `30-작업이력/` **17개 폴더** — 클릭 단위로 남김 |
| 서버 작업 출력 | `30-작업이력/17-GPU-docker+서버업데이트/01-실행-출력원문.txt` — ★화면이 없어 **명령 출력 원문**으로 대신함 |

★**변경한 것은 전부 변경 전 캡처 → 값 대조 → 변경 후 캡처 → 전수 검증** 순서로 진행했고,
조회만 한 항목에는 **"변경 0건"** 을 명시했습니다.
★서버(SSH) 작업은 캡처할 화면이 없어 **변경 전 조사 → 실행 → 변경 후 재조회**를 명령 출력으로 남겼습니다.
